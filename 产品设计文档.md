# 内在地形 · 产品设计文档

> 版本：v0.5 · 2026-07-31
> 状态：研究与讨论阶段
> 变更历史：
> - v0.1：初版（对话式 + 河流 + AI 周报）
> - v0.2：交互范式改为共处空间；新增智能自适应、语音对等、本地优先隐私架构、商业模式、目标用户
> - v0.3：隐私架构重写——承认 app+API 形态下"本地永不"不可兑现，改为"透明+不留存+用户控制+加密同步"；商业模式暂缓
> - v0.4：页面架构改为 5 Tab（共处/备忘/生活/发现/它）；共处页全能输入 + 分类可见可改 + 修正即学习机制；备忘和生活独立页完整规格；F2 风格初识问卷融入前 3 次聊天；记忆河从 Tab 降级为二级页
> - v0.5：共处页取消 AI 猜分类，改为**用户手动选三模式（倾诉/备忘/生活）+ 丝滑左右滑动切换**，从根源消除分类错误焦虑；记忆架构对齐业界前沿——升级为**四层递进认知层（Working→Episodic→Semantic→Procedural）+ Raw Ledger/Derived Views/Policy 三段论 + System1/2 双回路**，并引入每周睡眠巩固、艾宾浩斯遗忘曲线、三重检索（向量+图谱+时间）等机制

---

## 一、产品定位

### 一句话定位
**陪伴式自我记录 app —— 一个越来越懂你的 AI 角色，把你的日常倾诉沉淀成可回看的记忆。**

### 核心理念
不是习惯打卡工具，不是日记本，不是待办清单，是**"被一个越来越懂你的人记住"**这件事本身。

与传统产品的本质区别：
- 打卡工具：用户**完成任务**，AI 在背后**督促**
- 日记本：用户**记录事件**，没有**被理解**
- 待办清单：用户**管理任务**，工具是**冷酷的列表**
- 本产品：用户**向一个角色倾诉/记录任何事**，AI **理解并记住**，越用越懂你

用户每天打开 app 图的不是"完成感"，是**"被听见、被记住、被懂"**。

### 五个产品原则

1. **记录而非逼迫**
   不连续打卡、不断签警告、不积分惩罚。记忆只是"被留下痕迹"，不评判、不催促。备忘过期不红不催，只是淡一点灰一点。

2. **记忆可见可改**
   AI 记住的一切用户都看得见、改得动。这是信任的根基。任何"黑箱记忆"都会让用户感到失控。

3. **越用越懂**
   产品的核心价值随使用时长累积。第一周 AI 是陌生人，第三个月 AI 比你自己更懂你的模式。这是留存的核心壁垒。

4. **智能自适应（v0.2 新增）**
   **"不是死的，是根据用户"**。不仅记忆在累积，交互方式本身也要累积学习。市面上所有 AI 产品的交互参数都是死的——回复速度、回复长度、主动程度、话题深浅，全是产品经理定死的。本产品反过来：产品适应用户。

| 维度 | 智能自适应 |
|---|---|
| 回复节奏 | 学用户偏好——快型用户说完就回，慢型用户攒一段 |
| 回复长度 | 学用户耐受度——回长用户多写，回"嗯"用户说一句 |
| 主动程度 | 学用户孤独度——有些人喜欢被偶尔主动问候 |
| 话题深浅 | 学用户当时状态——疲惫时浅聊，精力足时深入 |
| 沉默容忍 | 学用户节奏——有些人 1 天不说话就是异常 |
| 表达密度 | 学用户风格——用户爱长句 AI 也长句 |
| 情绪温度 | 学用户当下状态——低落日更柔，得意日能接梗 |

**学习机制**：
- **隐式学习为主**：AI 观察用户反应自己调整（连续 3 次立刻接话 = 喜欢快）
- **显式调节为辅**：用户可主动说"你能不能别那么腻" → AI 立刻调整
- **学到就告知**：AI 不能偷偷变了，必须让用户知道"它因为我变了"
- **调整轨迹全程可见**：偏好档案里能看到 AI 何时因何调整了什么

**第一周"试探期"**：
AI 还没学到任何东西，用温和默认值 + 不被察觉的试探（有时快有时慢，有时长有时短），观察用户反应快速学习。第二周起基于学到的东西稳定运行 + 持续微调。

5. **三模式入口 + 事后可改（v0.5 全新替换"全能入口+分类可修"）**
   共处页输入区**左右滑动切换三模式**：📝 备忘 / 💭 倾诉 / 📷 生活。**分类完全由用户选模式决定，AI 绝不主动猜**，从根源消除"分错了"的信任危机。切换丝滑（300-500ms 渐变过渡，和呼吸节律对齐），已打的字切模式不丢失。事后在记忆河里仍可以改单条分类，改完记为偏好写进程序记忆。**"你选的就是对的，改一次它下次更懂你习惯选什么"。**

---

## 二、核心架构：记忆与角色解耦

这是整个产品的骨架。**记忆是"关于你的真相"，角色是"如何向你表达"**，两者分属不同维度，必须解耦。

### 为什么必须解耦

| 耦合的问题 | 解耦的好处 |
|---|---|
| 换角色=失忆，三个月白聊 | 换角色零损失，留存高 |
| 角色风格被记忆污染 | 记忆是中性事实，角色是表达滤镜 |
| 多角色并存不可能 | 同一份记忆可被多角色复用 |

### 解耦后的数据流（v0.5 更新：用户选模式，不再 AI 猜分类）

```
用户输入（共处页三模式切换，自己选分类）：
  💭 倾诉模式："今天加班到 23:47，累死了，回来还是读了两页书"
  📝 备忘模式：[时间辅助行填"周六10点"] + "买洗衣液"
  📷 生活模式：[月季照片] + "今天楼下看到的花，挺好看"
       ↓ （分类 = 用户选的模式，100% 确定，不猜）
┌──────────────────────────────────────────────────────────┐
│  System 1 · 快速回路（200-500ms，实时响应用户）            │
│  · 模式解析（倾诉/备忘/生活，用户已选）                    │
│  · 三重检索：向量 Top5 + 图谱 2-hop + 最近7天 → RRF融合   │
│  · 取 Top3 记忆 + Semantic/Procedural 已确认事实          │
│  · 注入 System Prompt + 生成 AI 回应                      │
│  · 备忘模式：额外提取到期时间 + 立刻注册本地通知           │
│  · 生活模式：额外 ASR/图像理解 + 生成缩略图/声纹 SVG       │
└─────────────────────────────────────────────┬────────────┘
                                              │ 异步写入请求
                                              ▼
┌──────────────────────────────────────────────────────────┐
│  System 2 · 慢速回路（后台低峰/定时跑，秒~分钟级）          │
│  · 写 Raw Ledger：Working → 7天后归档 Episodic            │
│  · 更新 Derived Views：向量索引/知识图谱/时间线/分类筛选   │
│  · 每周日 3 点：睡眠巩固任务                               │
│    - 扫描一周 Episodic，提炼 Semantic 事实候选             │
│    - 提炼 Procedural 交互偏好候选                          │
│    - 高置信(≥0.85)自动入，低置信(0.6-0.85)推发现页待确认   │
│    - 相似 Episodic 合并摘要，检测冲突标待处理              │
│  · 遗忘衰减：艾宾浩斯曲线降检索权重（永不物理删除）         │
└─────────────────────────────────────────────┬────────────┘
                                              │ 记忆层
                                              ▼
┌───────────────────────────────────────────┐
│  四层递进认知记忆（中性事实，与角色无关）    │
│  1. Working（会话级）                      │
│  2. Episodic（情景级 = 记忆河里的石子）     │
│  3. Semantic（语义事实 = 画像/洞察）        │
│  4. Procedural（程序偏好 = 风格/参数/习惯） │
└───────────────────────────────────────────┘
       ↓ （同一份记忆，喂给不同角色层）
┌───────────────────────────────────────────┐
│  角色层（表达滤镜，可换）                     │
│  - 默认温柔风：温柔回应用户                  │
│  - 损友风：幽默吐槽                           │
│  - 朋友风：直接闲聊                           │
│  风格可调，风格变化写入 Procedural 轨迹可见   │
└───────────────────────────────────────────┘
```

**同一份四层记忆，不同角色有不同的表达方式。** "懂你"的是底层 Semantic/Procedural，"怎么聊"是上层风格。
**Episodic 里的情景石子（倾诉/备忘/生活）都喂画像、洞察、周报三个回环。** 备忘不会成死数据，生活碎片不会只被看一次。

---

## 三、信息架构：四层递进认知记忆（v0.5 全面重写，对齐业界前沿）

记忆不是黑箱，也不是一锅粥。分**四层递进**——从瞬时工作记忆到永久程序记忆，每层职责清晰，每层用户都看得见、改得动。设计参考 agentmemory 四层认知架构 + 蚂蚁 Raw Ledger/Views/Policy 三段论。

### 层级总览

```
四层递进认知记忆（升层路径：Working → Episodic → Semantic + Procedural）
├─ 1. Working 工作记忆（会话级，瞬时）   "眼前正在说的事"
│    └─ 会话结束 / 7天后自动归档 →
├─ 2. Episodic 情景记忆（永久石子）     "那天发生了什么" ← 记忆河里的石子就是这个
│    └─ 每周 System 2 睡眠巩固 →
├─ 3. Semantic 语义记忆（稳定事实）     "关于我，有哪些确定的事" ← 画像/洞察
└─ 4. Procedural 程序记忆（决策偏好）   "它习惯怎么跟我交互" ← 风格/参数/分类习惯
```

**配套的记忆 OS 三段论（蚂蚁架构）：**
```
┌───────────────────────────────────────────────────────────────┐
│  Raw Ledger（权威原始账本）→ Derived Views（派生检索视图）     │
│       ↑                              ↓                        │
│       └────── Policy（控制策略引擎）←──┘                      │
│  Policy 决定：写什么/读什么/何时巩固/何时遗忘/冲突怎么判       │
│  所有 Policy 决策写进 Ledger 日志，可回放可审计可 A/B          │
└───────────────────────────────────────────────────────────────┘
```

---

### 第 1 层：Working 工作记忆（会话级瞬时）

**作用**：当前会话的"操作台"，System 1 实时读写。

**内容**（三模式分别存）：
- 倾诉：用户文字/语音原文 + AI 回应原文 + 当前会话上下文（最近 10 轮）
- 备忘：未提交的备忘草稿 + 辅助行的时间/重要度临时值
- 生活：刚上传未发送的图/语音临时文件 + 正在写的文字

**生命周期**：
- 当前会话全程保留
- 会话结束（用户切出 app 超过 10 分钟）或 7 天后 → 自动归档为 Episodic 情景记忆
- 用户手动"发了"的内容 → 立刻同时生成 Episodic 条目 + Working 保留一份

**用户控制**：会话中可以删单条消息、清草稿。Working 不入任何长期检索视图。

---

### 第 2 层：Episodic 情景记忆（永久石子 = 记忆河里的）

**作用**：构成河流/记忆线的主体。是用户每天打开 app 看到的"我经历了什么"。每条石子 = 一次完整的交互情景。

**内容**：每条情景记忆包含（v0.5 更新：分类 = 用户选的模式，非 AI 猜）：
- 时间戳
- **石子分类**：倾诉 💭 / 备忘 📝 / 生活 📷（用户选模式决定，事后可改，改完记修正轨迹）
- 情景摘要（AI 提取，一句话）
- 情绪标签（AI 提取 + 用户可改）
- 关键实体：人/地点/活动/作品（用于构建知识图谱 View）
- 原始全文（用户原文 + AI 回应原文，可展开看）
- **来源模式**：共处页 / 备忘页 / 生活页（用户从哪个 Tab 发的）
- **分类修正记录**（如有）：原分类是什么、用户何时改成了什么
- 专属字段：
  - 备忘类：`memo_due_time`（到期时间）/ `memo_status`（待完成/已完成/过期）/ `memo_remind_offset`
  - 生活类：`media_type`（图/音/文+图/图+音）/ `media_paths`（本地路径数组）/ `media_thumb`（缩略图/声纹 SVG）/ `ai_description`

**三类石子的区分处理（v0.5 不变，只是分类从 AI 猜变用户选）**

| 处理项 | 倾诉 💭 | 备忘 📝 | 生活 📷 |
|---|---|---|---|
| AI 是否回应 | 是，正常回应 | 简单回一句"好，记下了，到点叫你。"（不做情绪长回） | 有文字就回一句情绪反应；只发图音不发文字就不回，直接入库 |
| 是否设到期提醒 | 否 | 是，提取时间 + 系统本地通知 | 否 |
| 进入【备忘】页独立视图 | 否 | 是，分段展示（过期/今日/本周/更远） | 否 |
| 进入【生活】页瀑布流 | 否 | 否 | 是，图/声纹/文字精美呈现 |
| 喂 Semantic/Procedural 巩固 | 是 | 是（AI 会提炼备忘习惯、发现页提完成率洞察） | 是（提炼生活模式、洞察提关联） |
| 用户能改分类 | 能（改完写进 Procedural 偏好） | 能 | 能 |

**示例 · 三类情景石子**：
```
┌─────────────────────────────────────────┐
│ 倾诉 💭 · 7/29 周二 23:52             │
│ 摘要：加班到 23:47，回家读了《人间失格》两页│
│ 情绪：疲惫 → 平静                         │
│ 实体：加班、《人间失格》、阅读               │
│ 原文："今天加班到 23:47，回来累死了，        │
│       但还是读了《人间失格》两页，读完反而平静了。"│
│ AI 回应："你昨晚那么晚回来还看书，            │
│          我知道你是想给自己留点什么。"        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 备忘 📝 · 7/30 周四 09:12             │
│ 摘要：周六 10:00 前买洗衣液                 │
│ 情绪：中性                                 │
│ 实体：购物、家务                            │
│ 到期时间：2026-08-02 10:00                │
│ 提醒：提前 60 分钟 + 系统通知               │
│ 原文："周六 10 点前买洗衣液"                │
│ AI 回应："好，周六 10 点前帮你记着，          │
│          到点叫你。"                        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 生活 📷 · 7/30 周四 18:05            │
│ 摘要：楼下围栏边的粉色月季                  │
│ 情绪：平静 · 欣赏                          │
│ 实体：花、自然、粉色                        │
│ 图片：IMG_20260730_1805.jpg（附缩略图）      │
│ AI 写的描述："楼下围栏边开得正盛的粉色月季"   │
│ 原文："今天楼下看到的花，挺好看"             │
│ AI 回应："好巧，这条路平时可能不会注意。       │
│          花看着很温柔。"                     │
└─────────────────────────────────────────┘
```

**用户控制**：可编辑摘要、改情绪、**改石子分类（三模式互转）**、删整条、加批注。改分类记一条 Policy 日志（用户于 X 时将 Y 从 A 改为 B）。

**衰减机制（艾宾浩斯曲线，只降检索权重永不删）**：
- 权重：1 天 = 1.0 → 7 天 = 0.8 → 30 天 = 0.5 → 90 天 = 0.3 → 365 天 = 0.1
- 视觉效果：0-7 天完全清晰；7-30 天摘要清晰原文要点开；30-90 天摘要变淡（视觉微模糊）；90 天 + 进"远雾区"
- 用户手动打开/检索一次 → 权重立刻回弹到 0.9（相当于"艾宾浩斯复习一次"）

---

### 第 3 层：Semantic 语义记忆（稳定事实）

**作用**：AI 真正"懂你"的核心层。是从大量 Episodic 里提炼出的**稳定、不反复、可验证的事实**。System Prompt 常驻注入的就是这层。

**内容**（全部带"来源石子列表"，点进去能看原始证据，不信可以删）：
- 基础画像事实（从倾诉/备忘/生活抽）：行业、岗位、作息、居住状态、宠物、重要关系人、地理位置、长期目标……
- 稳定偏好事实（从倾诉抽 + 用户确认）：不喜欢吃香菜、失眠时听白噪音、焦虑时会散步、讨厌被提"相亲"……
- 关联规律洞察（从三类石子联合挖，即原"模式发现层"）：
  - 关联："你拍了猫的日子，后面阅读和散步记录也多"
  - 规律："你月末总有 3 天加班，倾诉里疲惫多，备忘完成率低"
  - 趋势："你最近 2 周晚上 1 点后的记录明显少了——是不是作息调整了？"
  - 矛盾："你说想早睡，但最近 5 天的倾诉都过了 1 点"
  - 备忘统计："你 80% 的备忘是周日晚上写的——习惯在周末规划下周"
  - 生活模式："每周六下午你都在咖啡馆——这条生活地点统计来自 12 条生活石子"
- **置信度 + 状态**：
  - 自动入（置信度 ≥ 0.85）：高确定事实，状态 = 已确认
  - 待确认（置信度 0.6-0.85）：推【发现页-关于我】让用户确认/否认/补充
  - 用户确认：状态 = 手动确认（优先级最高）
  - 冲突：新旧事实矛盾，状态 = 冲突待处理，推发现页问用户

**示例**：
```
Semantic 事实卡片 1
· 内容：独居，养了一只橘猫，叫"饭团"
· 来源石子：E023（7/15 生活：饭团蹲冰箱上）、E031（7/18 倾诉：今天饭团打翻了杯子）
· 置信度：0.92（自动入）
· 状态：用户已确认（7/20 用户在发现页点了"对的"）
· 用户可操作：编辑 / 标记过时 / 删除

Semantic 事实卡片 2
· 内容：你月末加班多，那时备忘完成率约 50%
· 来源石子：过去 8 周 32 条备忘 + 17 条倾诉
· 置信度：0.74（待确认）
· 状态：在发现页等待用户判断
```

**用户控制**：发现页 Tab 2「关于我」和 Tab 1「AI 注意到」统一管理。每条可确认/否认/补充/编辑/标过时/删除。否认的事实立刻标为已废弃，不再注入。

**冲突处理 Policy**：新候选与已确认事实矛盾时（已确认"爱猫"，新候选"对猫过敏"），发现页弹冲突卡片：「之前你说过养猫叫饭团，现在这条是"对猫过敏"。哪个对？还是都对（比如分阶段）？」——两边都保留证据链，用户选完修正冲突。

---

### 第 4 层：Procedural 程序记忆（决策偏好 = 它怎么跟我交互）

**作用**：AI 的"肌肉记忆"——怎么说话、什么时候回、提醒默认多久、分类习惯选什么……直接影响每一次回应的参数。和原"偏好档案层"类似，但是**升层为第四层认知记忆，明确从 Episodic 巩固而来**。

**内容**（全部带"什么时候、因为什么事件调整"的轨迹，用户可见）：
- 风格偏好 + 强度：温柔 70% + 直接 30%（可调可覆盖）
- 话题偏好：可深入话题列表 / 回避话题列表（用户标过的永不提）
- 称呼方式：怎么叫用户
- 表达节奏：长句/短句、是否 emoji、是否反问
- **6 个交互参数（智能自适应核心）**：
  - 回复节奏：准即时 / 攒一段再回 / 异步信件式
  - 回复长度：一句 / 一段 / 多段
  - 主动程度：从不 / 偶尔 / 高频
  - 沉默容忍：1 天 / 3 天 / 7 天
  - 情绪温度：克制 / 温和 / 亲密
  - 表达密度：用户长句 AI 也长句 的强度
- **备忘提醒默认参数**：提前提醒时间（默认 60 分钟）、是否系统通知、过期提几次（默认 2 次）
- **分类习惯（v0.5 新增，替代 v0.4"修正样本"）**：
  - 用户改分类的全部历史轨迹（原文、原分类、改成了什么、时间）
  - System 2 从轨迹里提炼："用户说'帮我记 XX' + 有时间词时，90% 概率会从倾诉改成备忘"——记为分类习惯偏好，下次在共处页切到倾诉模式但输入内容高度像备忘时，AI 可以在输入区右上角闪一个极淡的"📌？"（不打扰，用户点了就切备忘模式）——**不主动改分类，只做极弱的提示**。
- **调整轨迹时间线**：所有参数因为什么事件、在什么时候调了多少，逐条可见。

**示例**：
```
Procedural 偏好（当前生效值）
· 风格：温柔 70% + 直接 30%
· 称呼：叫"你"，不用昵称
· 表达：短句为主，少 emoji
· 6 参数：
  - 回复节奏：准即时（7/25 从"攒一段"调整，因为连续 3 次立刻接话）
  - 回复长度：一段（默认，待学）
  - 沉默容忍：3 天（默认，待学）
· 备忘提醒：提前 60 分钟 + 系统通知 + 过期提 2 次
· 分类习惯：
  - 输入有"明天/别忘了/叫我XX"且是倾诉模式时 → 弱提示"📌？"
  - （7/29 从 2 次用户改分类里巩固出的，来源 Episodic E047、E051）
· 回避：不主动提"我妈"（用户 7/14 标记过）
· 近期调整轨迹（可撤销）：
  - 7/25 回复节奏：攒一段 → 准即时（行为信号：连续 3 次用户 10s 内接下一句）
  - 7/22 毒舌：0% → 20%（显式反馈：用户说"最近想被怼一点"）
  - 7/15 温柔：90% → 70%（显式反馈：用户说"别那么腻"）
```

**用户控制**：【它页 Tab 1「它怎么对我」】手动覆盖所有参数；【它页 Tab 2「它因为我变了」】逐条撤销调整；分类习惯里的弱提示可以关闭。

**学习机制 Policy**：
- 行为信号触发（连续 3 次）→ 插值调整（α=0.2，不跳变）→ 阈值超过 20% 才告知用户 → 轨迹入账
- 用户显式说"你能不能 XX" → 立刻按用户的调（跳变允许）→ 告知 → 轨迹入账
- 用户改分类 → 分类修正轨迹入账 → System 2 每周巩固提炼分类习惯偏好
- 第一周试探期：节奏/长度在默认值 ±15% 之间不被察觉地交替试，观察行为信号快速收敛

---

## 四、核心流程

### 主流程（每天 · 共处页三模式丝滑输入）—— v0.5 重写：用户选模式，AI 不猜分类

```
打开 app → 共处首页
   ↓
[共处空间] —— 雾里的光在缓慢呼吸，7.2 秒真人呼吸节律
   ↓
输入区默认在「💭 倾诉」模式，三指示点居中
   ├─ 点指示点 / 手指左右滑输入区，切模式（300-500ms 渐变过渡，对齐呼吸节律）
   │    ├─ 左滑到「📝 备忘」：淡蓝色日光背景，输入框下方弹出辅助行（时间解析 + 重要度）
   │    ├─ 保持「💭 倾诉」：淡桃色暖光，基础高度，只有输入
   │    └─ 右滑到「📷 生活」：淡绿色晨光背景，左侧大相机图标显著，可加地点语音
   └─ 切模式时，已打的字 / 已选的备忘辅助值 / 已上传的图音 → 全部保留，不丢失
   ↓
用户在当前模式下输入（打字 / 语音 / 加图音 / 备忘填辅助行）
   ↓
AI "在听"：光微微偏向用户、雾散开一点 + 输入区状态提示
   ↓
点发送 → 分类 = 当前模式（100% 确定，不猜）
   ↓
System 1 · 快速回路（200-500ms 内返回）：
   ├─ 三重检索取记忆 Top3 注入上下文
   ├─ Semantic 已确认事实 + Procedural 当前偏好 → 常驻 System Prompt
   ├─ 倾诉类：生成个性化情绪回应 + 提取事件/情绪/实体
   ├─ 备忘类：简单回"好，记下了，X点叫你" + 提取到期时间 + 立刻注册本地通知
   ├─ 生活类：有文字则回一句情绪反应 + ASR/图像理解写描述 + 生成缩略图
   └─ （分类习惯匹配到内容高度像另一个模式 → 右上角淡闪"📌？/💭？/📷？"不打扰）
   ↓
AI "在想"：光在缓慢聚拢（最小 1.2s，节奏 = Procedural·回复节奏偏好）
   ↓
AI 回应浮现（文字浮在雾上 / 语音从光里传出）
   ↓
异步写 System 2：
   ├─ Working 保留当前会话
   ├─ 立刻生成 Episodic 情景石子 → 入记忆河 + 对应独立页同步
   │    ├─ 倾诉 💭 → 只入记忆河
   │    ├─ 备忘 📝 → 记忆河 + 【备忘】页分段出现
   │    └─ 生活 📷 → 记忆河 + 【生活】页瀑布流出现卡片
   └─ Ledger 写入日志 + 更新 Views（向量/图谱/时间/分类）
   ↓
AI 安静下来，不追问，等用户继续
   └─ （F2 风格初识问题，前 3 次聊天结束 2 秒后淡字弹出，有跳过）
```

**关键节点说明（v0.5 全新）**：

- **分类 100% 用户选是信任核心**：用户选的就是对的，AI 绝不替用户改，消除"分错了 AI 很傻"的负面感受。
- **切换丝滑是体验命门**：300-500ms 渐变和呼吸节律对齐，文字不丢，辅助状态不丢。用户感觉"在一个输入区变形态"，不是"切了三个页面"。
- **弱提示替代强修正**：内容明显像别的模式时只淡闪不打扰，主动权永远在用户。这比"AI 直接帮你改分类"高级。
- **用户事后改分类 = 一次学习样本**：改完写轨迹，System 2 巩固进 Procedural 分类习惯，下次更精准地弱提示。
- **石子在记忆河 + 独立页双向同步**：备忘不会因为是共处页记的就不在备忘页；生活碎片不会因为共处页发的就不在瀑布流。

**F2 风格初识的细节（同 v0.4，不变）**：
分散在前 3 次聊天收尾问：
- 第 1 次聊完 2 秒后：「最后问一句，你喜欢我怎么跟你说话？」【温柔点 / 直接点 / 幽默点 / 跳过】→ Procedural·风格初始值 + 轨迹入账
- 第 3 次聊完 2 秒后：「对了，你希望我回快一点，还是攒一段时间再说？」【快一点 / 攒一段 / 你帮我判断 / 跳过】→ Procedural·回复节奏初始值 + 轨迹入账
- 选项永远有"跳过"，选完 AI 回"好，记住了，以后不满意随时说。"

---

### System 2 · 睡眠巩固流程（每周日凌晨 3:00，后台低峰跑）

v0.5 新增，对应人类睡觉时大脑整理记忆。

```
每周日 3:00
   ↓
扫描过去 7 天所有 Episodic 情景石子
   ↓
Step 1 · 去重合并：
   语义相似度 > 0.9 的石子 → 加"关联石子"标记（不删原文，保留证据链）
   ↓
Step 2 · 提炼 Semantic 事实候选：
   LLM 扫 Episodic，输出带置信度的候选事实列表 + 每条的来源石子 ID
   ├─ 置信度 ≥ 0.85 → 自动入 Semantic，状态 = 已自动确认
   └─ 置信度 0.6 ~ 0.85 → 写入 Semantic 待确认池 → 推【发现页 - 关于我】
   ↓
Step 3 · 提炼 Procedural 偏好候选：
   ├─ 行为信号扫描：过去 7 天连续 3 次立刻接话 / 连续 3 次回"嗯"… → 调 6 参数
   ├─ 分类修正扫描：过去 7 天用户改了几次分类 → 提炼分类习惯偏好（XX 内容 → YY 模式的弱提示规则）
   ├─ 调整幅度 ≥ 20% → 告诉用户（下次共处聊天时淡字提一句 + 轨迹入账）
   └─ 调整幅度 < 20% → 直接生效不告知，只轨迹入账
   ↓
Step 4 · 冲突检测：
   新候选事实 vs Semantic 已确认事实 → 冲突 → 推【发现页 - 冲突卡片】问用户
   ↓
Step 5 · 遗忘衰减权重批量更新：
   所有 Episodic 石子的检索权重按艾宾浩斯曲线重算（永不删数据）
```

---

### 周期流程（每周信件 + 备忘提醒）

**周报信件流程（基本不变，改为从四层记忆生成）**：
```
周日傍晚
   ↓
System 2 扫描本周 Episodic + Semantic 新增事实 + Procedural 调整
   ↓
生成四段式周报信件（文学感叙述）：
   1. 被看见：复述本周，三类石子都提，带情绪色彩
   2. 备忘小回顾：本周备忘完成率、未完成的 1-2 条、"要不要挪到下周一？"
   3. 被发现：本周 1-2 个新提炼的 Semantic 关联洞察，告诉用户"我注意到"
   4. 被允许：一句温柔的松动，不需要有进展也可以
   ↓
推送给用户 + 点备忘挪时间 → 跳备忘页自动改 + 洞察点确认 → 跳发现页
```

**备忘提醒流程（同 v0.4 不变）**：
```
到期前 N 分钟（N = Procedural·备忘提醒·提前时间）→ 系统本地静默通知
   ↓
当天用户打开共处页说话时 → AI 闲聊里轻轻提一句："你今天要买洗衣液，买了吗？"
   ↓
当天 23:59 未完成 → 备忘页移"已过期"段，红淡不刺眼
   ↓
后面 2-3 天如果聊天，AI 可能提一次（默认总共提 2 次，超过就不再提）
```

---

### 深度流程（偶尔 · 去 5 Tab / 记忆河）

```
用户想"翻翻过去/看看规律/还有什么要做"
   ↓
底部 5 Tab：
├─ 📝 备忘页：分段（过期/今日/本周/更远）+ 顶部加备忘（直接加不用回共处）+ 完成/改时间/展开看原文+AI回应/跳记忆河
├─ 📷 生活页：双列瀑布流 + 月统计 + 顶部加碎片（直接加）+ 点卡片详情沉浸 + 关联当日倾诉石子跳转记忆河
├─ ⭐ 发现页：
│    Tab1「AI 注意到」：Semantic 关联洞察卡片（确认/否认/补充）
│    Tab2「关于我」：Semantic 画像事实卡片（确认/否认/补充/标过时） + 冲突处理卡片
│    顶部「记忆河」入口 → 进入二级全记忆时间线
├─ 👤 它页：
│    Tab1「它怎么对我」：Procedural 参数覆盖（风格 / 6 交互参数 / 备忘提醒 / 弱提示开关）
│    Tab2「它因为我变了」：Procedural 调整轨迹（可逐条撤销）
│    底部设置：隐私 / 云盘同步 / 通知 / 语音 / 导出删除
└─ 「记忆河」二级页（共处页左滑 / 发现页入口）
     · Episodic 全部石子按时间一条线
     · 筛选：全部/倾诉/备忘/生活/情绪/关键词
     · 远雾区（>90天）/ 浅石区（30-90天）/ 清晰区（<30天）视觉衰减
```

---

## 五、页面架构：5 个 Tab（v0.5 不变，共处职责更新）

```
┌─────────────────────────────────────────────────────────────┐
│  底部 5 Tab：                                                │
│                                                               │
│  🏠 共处      📝 备忘      📷 生活      ⭐ 发现      👤 它     │
│  (首页)                                                      │
└─────────────────────────────────────────────────────────────┘
```

### 各 Tab 职责（v0.5 共处职责更新）

| Tab | 进入场景 | 主功能 | 是否需要记忆河入口 |
|---|---|---|---|
| 🏠 **共处**（首页） | 每天打开、想说话、想随手记 | **三模式输入（📝/💭/📷 左右滑切换 + 丝滑过渡）+ AI 回应 + 光状态 + 左滑进记忆河 + 模式弱提示** | 是（左滑） |
| 📝 **备忘** | 要看待办、要加新备忘、要改旧备忘 | 分段展示备忘 + 顶部加备忘 + 完成/改时间 + 展开原文+AI回应 | 是（点石子详情跳记忆河） |
| 📷 **生活** | 要翻照片、要加新碎片、要回顾生活 | 瀑布流精美呈现 + 顶部加碎片 + 月统计 + 详情沉浸 | 是（点碎片→关联当日石子→进记忆河） |
| ⭐ **发现** | 想看看 AI 发现了什么规律、想看看 AI 怎么看我 | Semantic 洞察卡片流 + 画像事实确认 + 冲突处理卡片 + 记忆河顶部入口 | 是（顶部明确入口） |
| 👤 **它** | 要调 AI 风格/参数、看它怎么变了、改设置 | Procedural 参数覆盖 + 调整轨迹 + 隐私/同步/通知/账号设置 | 否 |

记忆河为二级交叉页。

---

## 六、视觉与交互设计（v0.5 重点更新：共处输入区三模式切换）

### 整体风格（不变）

- **玻璃拟态**：毛玻璃卡片层叠 + 半透明 + 模糊背景 + 柔光高光
- **深色为主 + 暖橘强调色**：深墨蓝背景，暖橘/暖黄作为情绪强调
- **衬线字体**：思源宋体（中文）+ Cormorant Garamond（英文斜体小字），文学感
- **不对称布局**：杂志感错落排布，留白有呼吸
- **微交互**：按下涟漪、光点呼吸（真人 7.2s 节律）、水流缓慢流动
- **真实呼吸节律**：吸 2s→屏 1s→呼 3.7s→静 0.5s，7.2s 周期；performance.now() 算真实 delta；所有动效跟呼吸节律对齐（比如模式切换发生在屏气/静息的 1.5s 窗口内，不打断节奏）

---

### Tab 1 · 共处空间（首页）—— v0.5 全面重写：三模式丝滑切换

**默认视图。每天打开就看到雾+呼吸光。**

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│                                                          │
│               🌫️ 雾里的光（7.2s 真人呼吸）                 │
│                                                          │
│       AI 回应文字 / 用户刚说的话 浮现在雾上                │
│                （极淡的"📌？"弱提示可能在右上角落）          │
│                                                          │
│                                                          │
│                  📝   ●   💭   ○   📷    ← 3 模式指示点  │
│                       （选中点放大+发光+呼吸联动）         │
│                                                          │
│  极淡引导字（首次打开 3 秒淡出）：                         │
│      "左滑备忘 · 中间倾诉 · 右滑生活"                     │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │  （背景光晕随模式 300ms 渐变：                        │  │
│  │   📝备忘=淡蓝日光  💭倾诉=淡桃暖光  📷生活=淡绿晨光） │  │
│  │                                                    │  │
│  │   占位文字（淡入淡出随模式变）：                     │  │
│  │    📝 = "要记什么、什么时候…"                      │  │
│  │    💭 = "和它说点什么…"                            │  │
│  │    📷 = "今天的碎片…"                              │  │
│  │                                                    │  │
│  │   ── 辅助字段区（200ms 滑入滑出）──                 │  │
│  │   📝模式：时间自然语言解析行 + 重要度三档圆点        │  │
│  │   💭模式：无                                       │  │
│  │   📷模式：淡"加语音/加地点" + 左侧大📷图标显著      │  │
│  │                                                    │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  [🎤按住说]  [📎加图/音]                 [发送]          │
│  （生活模式 🎤→🎙️录音，📎→📷相机/相册更显眼）              │
└──────────────────────────────────────────────────────────┘
```

**模式切换的丝滑细节（v0.5 核心交互）**：
1. **双触发**：点 3 个指示点 / 手指在输入区内左右滑（距离 > 30px 触发）
2. **内容不丢**：已打文字、备忘模式填的时间、已上传图音 → 切模式全保留；切出备忘时辅助字段灰掉但不丢，切回来还在
3. **过渡节奏**：
   - 背景光晕颜色：CSS 300ms ease-out 渐变（和呼吸"屏气"窗口对齐，呼吸到第 2-3 秒之间才允许触发切换，避免呼吸和切换抢视觉重心）
   - 占位文字：50ms 透明 → 换字 → 200ms 淡入
   - 辅助字段区：备忘从下 200ms ease-out 滑入 / 滑出；生活同理
   - 输入框高度：备忘模式（有辅助行）比基础高 40px，300ms 丝滑伸缩
4. **首次引导**：3 个指示点上方一行极淡字 3 秒后淡出。之后不再弹。
5. **分类习惯弱提示**：内容明显像另一个模式时（Procedural·分类习惯里匹配到高概率规则），输入区右上角闪一个极淡的"📌？"（透明度 0.4，不晃），用户点了立刻切到那个模式，不点 5 秒后自动消失。永远不主动替用户切。

**AI 在场状态（视觉规格同 v0.4，不变）**：安静→在听→在想→回应，四状态靠光和雾的微变化表达。

---

### Tab 2 · 📝 备忘页（同 v0.4 不变，补充一句话）

分段（过期/今日/本周/更远）+ 顶部智能体式输入（语/文）+ 完成/改时间 + 展开原文+AI回应+跳记忆河。
**顶部加备忘的模式=固定备忘模式**，不需要切换三模式。

---

### Tab 3 · 📷 生活页（同 v0.4 不变，补充一句话）

双列瀑布流 + 玻璃卡片（图/语音/组合三类精美规格）+ 顶部加碎片 + 月统计 + 详情沉浸。
**顶部加碎片=固定生活模式**。

---

### Tab 4 · ⭐ 发现页（v0.5 补：冲突处理卡片）

两个 Tab：
- Tab 1「AI 注意到」：Semantic 关联洞察卡片（是这样 / 不是 / 补充）
- Tab 2「关于我」：Semantic 画像事实（对吗？/ 不对 / 补充 / 标过时）+ **冲突处理卡片**（新旧事实矛盾时弹出："之前说养猫，现在说对猫过敏，选一下？"）
- 顶部「记忆河 →」入口

---

### Tab 5 · 👤 它页（v0.5 不变 + 补：分类习惯弱提示开关）

两个 Tab + 底部设置：
- Tab 1「它怎么对我」：Procedural 参数覆盖（风格环 + 6 交互参数 + 备忘提醒设置 + **分类弱提示开关**）
- Tab 2「它因为我变了」：Procedural 调整轨迹（每条可撤销）
- 底部设置：隐私透明面板 / 云端开关 / 云盘加密同步 / 通知 / 语音 / 导出删除 / 账号

---

### 二级页 · 记忆河（同 v0.4 不变，补：分类互转入口）

Episodic 全部石子按时间线，分类筛选，远雾/浅石/清晰区衰减视觉。
**单条石子长按 → 弹菜单：改分类（倾诉↔备忘↔生活）/ 编辑情绪 / 删 / 加批注** → 改分类立刻写 Policy 日志 + 下次巩固进 Procedural 分类习惯。

---

## 七、数据流向全景（v0.5 重写：含 System1/2 + Ledger/Views/Policy + 四层升层）

```
 ┌───────────────────────────────────────────────────────────────┐
 │  🏠 共处页（三模式用户选分类）   📝备忘页（直接备忘）   📷生活页（直接生活）│
 │          │                         │                  │          │
 └──────────┴─────────────────────────┴──────────────────┴──────────┘
                              │ 发送（分类100%确定）
                              ▼
 ┌───────────────────────────────────────────────────────────────┐
 │  System 1 · 快速回路（200-500ms）                              │
 │  · 三模式解析 → 备忘/倾诉/生活各自处理逻辑                    │
 │  · 读 Policy → 三重检索（向量/图谱/时间）→ RRF融合 → Top3 注入 │
 │  · Semantic 已确认事实 + Procedural 当前偏好 → 常驻 Prompt    │
 │  · 生成 AI 回应 + 备忘本地通知注册 + 生活缩略图/声纹生成       │
 └─────────────────────────────┬─────────────────────────────────┘
                               │ 异步写入（System 2 收）
                               ▼
 ┌───────────────────────────────────────────────────────────────┐
 │  Raw Ledger（权威原始账本 = 四层记忆本体）                     │
 │  ├─ Working 层：当前会话（7天后自动归档）                      │
 │  ├─ Episodic 层：情景石子（永久保留） ←←← 用户改分类写这里     │
 │  ├─ Semantic 层：事实画像洞察（来源石子ID链，可追溯）          │
 │  └─ Procedural 层：交互偏好 + 风格 + 分类习惯 + 调整轨迹       │
 │                                                                 │
 │  Policy 控制策略引擎（所有决策记录回写 Ledger 日志）：          │
 │  · 写策略：什么时候升层 / 什么置信度自动入 / 冲突怎么判        │
 │  · 读策略：检索权重计算 / RRF融合参数 / 会话分散去重            │
 │  · 巩固策略：每周日 3:00 跑 / 候选阈值 0.6 & 0.85             │
 │  · 遗忘策略：艾宾浩斯曲线 / 手动捞起权重回弹 0.9               │
 └─────────────────────────────┬─────────────────────────────────┘
                               │ 派生更新（异步）
                               ▼
 ┌───────────────────────────────────────────────────────────────┐
 │  Derived Views（只用于检索，不存真相，全部可追溯回 Ledger）     │
 │  · 向量索引（pgvector）→ 语义相似度检索                      │
 │  · 简化知识图谱（实体-关系边）→ 2-hop 关联检索                │
 │  · 时间线视图 → 按时间分区查（记忆河/周报用）                 │
 │  · 分类筛选视图 → 按 倾诉/备忘/生活 + 备忘状态 + 情绪筛选    │
 └───────────────┬──────────────────────────────┬────────────────┘
                 │                              │
                 ▼                              ▼
   ┌─────────────────────────┐   ┌─────────────────────────────┐
   │ 📝备忘页 │ 📷生活页 │ ⭐发现页 │   │  System 1 每次回应读这里   │
   │ （按分类视图取各自石子）   │   │  （Policy 管取 Top3 注入）    │
   └────────────┬────────────┘   └─────────────────────────────┘
                │ 点石子跳 ↓
                └──────────────────► 记忆河二级页（时间线视图）
                                       │
                                       └──► 所有石子/事实/偏好 最终喂回共处 AI
```

---

## 八、角色系统设计（同 v0.4，不变）

### MVP 方案：单一默认角色 + 可调风格 + F2 初识问卷
默认角色：无名字/性别/人设，风格默认温柔 70% + 直接 30%。用户通过 F2 前 3 次聊天 + 聊天显式说 + 隐式行为学习 + 【它页】手动覆盖 四条路径调 Procedural·风格参数。

### 未来扩展（非 MVP）
多角色并存（同一份四层记忆多个表达滤镜）/ 角色模板分享 / 演化叙事。

---

## 九、隐私与数据归属（同 v0.3，不变 + v0.5 补一条）

核心承诺：
1. 透明可见 — 数据流走向面板随时看
2. 不留存不训练 — 云端即用即销毁
3. 用户控制 — 关云端/删/导出
4. 加密同步 — 用户云盘端到端加密
5. 分级敏感 — 极敏感特殊路径
6. **v0.5 新增：分类修正轨迹和 Procedural 偏好仅本地存储 + 加密同步，不单独上传云端**

---

## 十、商业模式（暂缓，不变）

先做到"离不开"。

---

## 十一、目标用户与差异化（v0.5 更新差异化壁垒）

### 核心目标用户（不变）
既孤独又想成长的 22-35 岁独居上班族 + 想自我成长 + 不爱写日记但想记的轻量用户。

### 差异化壁垒（v0.5 升级顺序）

1. **共处空间范式 + 7.2s 真人呼吸** — 第一眼看就不一样。
2. **三模式丝滑切换 + 用户选分类 + 弱提示不越界** — 别的 AI 猜分类错了用户只能忍；我们让用户自己选，错了（事后改）是一次教 AI 的机会。信任根基不同。
3. **四层递进认知记忆 + 每周睡眠巩固 + Ledger/Views/Policy 三段论** — 对齐 2025 业界前沿架构。Episodic 完整保留 + Semantic/Procedural 提炼 + 全部带证据链可追溯。
4. **艾宾浩斯遗忘曲线 + 永不物理删除 + 手动捞起权重回弹** — 记忆像人一样会淡，但不会消失，主动想起就更清晰。
5. **三类石子一条河 + 备忘生活独立页双向同步** — 备忘不是死列表，生活不是冷相册，都是记忆的一部分。
6. **越用越懂 + 智能自适应 + 调整轨迹全程可见可撤销** — 参数不是死的，是学出来的，变了就告诉用户。
7. **记忆可见可改 + 记忆与角色解耦** — 换风格不失忆，一切 AI 记住的用户都能改。

---

## 十二、MVP 范围（v0.5 大更新：对应新架构的最小可用）

| 模块 | MVP 范围（最小可用） | MVP 暂不做 / 简化 |
|---|---|---|
| **共处首页** | 雾里的光 + 7.2s 呼吸 + 状态机 + **三模式左右滑/点双触发丝滑切换（立刻触发+动画速率对齐呼吸，内容不丢）** + 辅助字段区（备忘时间行+重要度，生活相机+地点）+ 语/文/图音输入 + **分类弱提示📌？（opacity 0.25极淡+50%概率+30次后自动关+首次带关掉按钮）** + 左滑进记忆河 | 复杂粒子天气；F2 第 5 次打扰问题（只问前 2 个） |
| **F2 风格初识** | 前 3 次聊完淡字问 2 题（风格 + 回复节奏）+ 跳过选项 | 集中问卷 UI；第 5 次打扰问题 |
| **System 1 · 快速回路** | 一次 LLM 调用：根据模式生成回应 + 提取情绪/实体 + 备忘提取时间 + 生活ASR/描述；**常驻 Semantic 已确认事实 + Procedural 偏好**；简化三重检索（先只向量 Top5，MVP 暂不做图谱和时间） | 知识图谱 2-hop 检索；RRF 融合（MVP 先向量+ BM25 两路） |
| **System 2 · 慢速回路** | Working→Episodic 自动归档 + **Consolidation（懒计算+缓存混合：进发现页/周报前触发，不过期就不重跑，不做全局Cron）** + **艾宾浩斯权重批量更新（该用户范围）** + Ledger 日志写入 | 复杂去重合并；冲突检测的智能判定（MVP 冲突先标出来让用户选，不自动判） |
| **Working 记忆** | 当前会话操作台（三类内容草稿/原文/上下文） | 会话级跨端同步（MVP 单端） |
| **Episodic 情景记忆** | 三模式石子结构 + 专属字段 + **分类事后可改（写修正轨迹）** + 衰减视觉（记忆河近清远雾） | 多媒体云存储（MVP 本地路径） |
| **Semantic 语义记忆** | 画像事实候选 + 洞察候选（LLM 每周扫提，不做真统计）+ 置信度两档（≥0.8自动入/0.6-0.8待确认）+ 0.8-0.85区间额外标「低置信自动入」徽章（发现页可否认） + **来源石子ID链可追溯** + 发现页 Tab1+Tab2 管理 | 高阶性格倾向分析；冲突自动合并（MVP 只弹窗问用户） |
| **Procedural 程序记忆** | 风格参数 + 2 个交互参数（节奏/长度）+ 备忘提醒设置 + **分类修正轨迹 + 分类习惯弱提示规则** + 调整轨迹可见 | 6 个参数全做（MVP 只做 2 个核心） |
| **📝 备忘页** | 分段（过期/今日/本周/更远）+ 顶部备忘模式直接输入（语/文）+ 完成/改时间 + 展开原文+AI回应 + 跳记忆河 | 重复/地点提醒/子任务 |
| **📷 生活页** | 双列瀑布流 + 玻璃卡片 + 图/音/组合三类 + 顶部生活模式直接加碎片 + 月统计 | 分享长图/视频；详情内嵌关联石子（MVP 先跳转） |
| **⭐ 发现页** | Tab1 洞察 + Tab2 画像确认 + **冲突处理卡片** + 记忆河顶部入口 | 自由拖拽星图 |
| **👤 它页** | Tab1 参数覆盖（风格 + 2 交互参数 + 备忘提醒 + **弱提示开关+累计次数显示**）；Tab2 调整轨迹可撤销；底部设置 + 隐私清空一切记忆三级确认入口 | 试说一句实时预览 |
| **记忆河二级页** | 时间线 + 分类筛选器 + 衰减视觉 + 单条长按菜单（改分类/编辑情绪/加批注/**永久删除二级确认**） | 双指缩放月/年切换 |
| **周报信件** | 四段式（含备忘回顾 + 洞察提及）+ 推送通知 | 自定义推送时间；信件分享长图 |
| **隐私架构** | 云端不留存声明 + 透明面板 + 云端可关 + 云盘加密同步（先做 iCloud / 坚果云其中一个） | 自建中转层；第三方审计；开源客户端 |
| **智能自适应** | 第一周试探期 + 行为信号隐式学习（2 参数）+ 分类改完写轨迹 + 调幅≥20% 告知 + 轨迹入账可撤销 | 6 参数高阶建模 |
| **危机干预** | 极敏感关键词正则 + 预设模板 + 标极敏感不入洞察/周报 | 援助机构地区化 |
| **备忘本地通知** | 到期前系统本地通知（iOS UNUser / Android Alarm）+ 加备忘立刻注册 + 卸载重装从云盘同步并重注册 | 复杂重复规则；节假日排除 |
| **语音全链路** | 三 Tab 录音 UI + ASR 云端 TBD + TTS 先不做（只文字回应）+ 生活声纹 SVG 本地生成 | 情绪合成 TTS；声纹石子动画 |
| **Derived Views** | 向量索引（pgvector）+ 全文索引（BM25）+ 时间线视图 + 分类筛选视图 | 简化知识图谱实体-关系边（MVP V2 再做） |

---

## 十三、技术关键点（v0.5 重写：对应新架构的地基）

### 1. 云端 API 不留存（同 v0.3）
选零保留供应商；后续自建中转层；透明面板；审计接口。

### 2. 共处页三模式丝滑切换（v0.5 新增前端关键）

**问题**：CSS 渐变 + 输入框高度伸缩 + 文字淡入淡出 + 内容不丢 + 跟 7.2s 呼吸节律对齐，要做到"感觉没切 Tab，只是一个输入区在变形态"。

**MVP 方案**：
- 一个 React/Vue 组件包整个输入区，state 管理：`mode`（memo/confide/life） + `inputText`（共享一个值） + `memoAux`（备忘辅助行） + `lifeMedia`（生活图音）
- 模式切换只改 state.mode，已有的 inputText/memoAux/lifeMedia **全部保留**（只是显示不显示的区别）
- 背景光晕 CSS `transition: background-color 300ms ease-out;`
- 输入框高度用 `transition: height 300ms ease-out;`，备忘模式高 40px
- 占位文字：mode 变化时先 `opacity:0`（50ms）→ 换文字 → `opacity:1`（200ms）
- **呼吸对齐（放宽约束）**：用户滑动/点指示点 → **立刻触发 mode 切换**，不等呼吸窗；只是切换动画的节奏速率对齐到当前呼吸阶段——屏气窗口(2-3s)内动画 1.2x 速，呼气窗口(3-6.7s)内 0.8x 速，体感上"立刻动了"但视觉节奏不跟呼吸打架。
- **分类弱提示（新手期+极低存在感）**：绝对定位右上角；`opacity: 0.25`（非常淡）+ 最小字号 11px；不闪动；命中规则后仅 **50% 概率弹出**；5s 自动 fade-out；累计输入满 30 次自动关闭（新手期结束）；第一次弹出时附带极小 `×` 按钮，tooltip 文案「不想看提示？永久关掉」；Procedural 偏好表存开关，用户可随时在【它页】开/关。

### 3. 四层记忆 + Ledger/Views/Policy（v0.5 核心数据架构）

**Postgres 表设计（MVP 最小）**：
```sql
-- 1. Raw Ledger：events 大表 = Episodic（情景石子）
--  Working 层不入 Postgres，走本地 SQLite / 端内 KV，会话结束归档进此表
CREATE TABLE events (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  stone_type TEXT NOT NULL CHECK (stone_type IN ('confide','memo','life')), -- 用户选的模式
  source_tab TEXT NOT NULL, -- 共处页/备忘页/生活页
  happened_at TIMESTAMPTZ NOT NULL,
  summary TEXT, -- AI 提取一句话摘要
  emotion TEXT[], -- 情绪标签
  entities JSONB, -- {people:[], places:[], activities:[], works:[]} → 后续图谱 View 的来源
  original_input JSONB NOT NULL, -- {text, audio_url, ...} 用户原文
  ai_response TEXT, -- AI 回应原文
  type_meta JSONB, -- 备忘类：{due_time, status, remind_offset}；生活类：{media_type, media_paths, thumb, ai_desc}
  correction_log JSONB[], -- [{from, to, at, reason}] 事后分类改了几次轨迹
  weight REAL NOT NULL DEFAULT 1.0, -- 艾宾浩斯检索权重，System2 每周更新
  deleted_at TIMESTAMPTZ, -- 软删除：非空 = 用户永久删除，所有检索/巩固/导出/视图一律过滤
  embedding vector(1536), -- 向量索引，Derived View 1
  created_at TIMESTAMPTZ NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL
);
-- 注：deleted_at 非空的行 = 用户永久删除记录。Ledger/审计日志里仍然留存删除动作（event_hard_deleted），可审计但语义数据对所有功能彻底不可见。

-- 2. Semantic 语义事实
CREATE TABLE semantic_facts (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  fact_type TEXT NOT NULL, -- profile / preference / insight
  content TEXT NOT NULL,
  confidence REAL NOT NULL,
  status TEXT NOT NULL CHECK (status IN ('auto_confirmed','manual_confirmed','pending','conflict','deprecated')),
  source_event_ids UUID[] NOT NULL, -- 来自哪些 Episodic 石子，可追溯
  conflict_with UUID, -- 如果 status=conflict，和哪条冲突
  user_note TEXT, -- 用户补充/否认时写的话
  created_at TIMESTAMPTZ NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL
);

-- 3. Procedural 程序偏好
CREATE TABLE procedural_prefs (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  pref_key TEXT NOT NULL, -- style_warmth / style_directness / reply_rhythm / reply_length / memo_lead_minutes / ...
  value REAL NOT NULL, -- 0~1 连续值或枚举码
  source TEXT NOT NULL, -- f2_init / explicit_feedback / behavior_signal / manual_override / correction_habit
  source_event_ids UUID[], -- 来自哪些事件/轨迹，可追溯
  adjustment_log JSONB NOT NULL, -- {from, to, at, delta} 每次调都记
  effective BOOLEAN NOT NULL DEFAULT true, -- 用户撤销后变 false
  created_at TIMESTAMPTZ NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL
);

-- 4. Ledger 审计日志（所有 Policy 决策全记，可回放）
CREATE TABLE ledger_actions (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  action_type TEXT NOT NULL, -- event_created / event_updated / semantic_candidate_created / procedural_adjusted / weight_decayed / retrieval_run ...
  action_detail JSONB NOT NULL, -- 具体参数
  policy_version TEXT NOT NULL, -- 哪版 Policy 做的决策，可 A/B
  created_at TIMESTAMPTZ NOT NULL
);

-- 5. Derived View：简化知识图谱（MVP V2 再做，先留空表占位）
CREATE TABLE kg_edges (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  subject TEXT NOT NULL,
  predicate TEXT NOT NULL,
  object TEXT NOT NULL,
  source_event_id UUID NOT NULL,
  weight REAL NOT NULL DEFAULT 1.0
);

-- Derived View 的向量索引用 pgvector 直接在 events.embedding 列建；时间索引用 events.happened_at + user_id 联合 BTree；分类筛选就是 events.stone_type + type_meta。
```

### 4. System 1：检索 + Prompt 注入（v0.5 新）

**MVP 简化：两路检索（向量 + 时间），不做知识图谱**
```
每次回应前 System1：
  1. 用户当前输入 → Embedding → events 表向量余弦 Top5
  2. events.happened_at >= now()-7天 → 取最近 10 条按 weight 排前 3
  3. 两路结果按 RRF(k=60) 融合去重 → 取 Top3
  4. Semantic 层 where status IN ('auto_confirmed','manual_confirmed') → 全部注入（一般 < 50 条事实，token 够）
  5. Procedural 层 where effective=true GROUP BY pref_key → 取最新有效值 → 拼入 System Prompt（"用户喜欢回复节奏=准即时，长度=一段，温柔70%直接30%..."）
  6. 拼 LLM Prompt：System + Semantic事实 + Procedural偏好 + 最近3轮上下文 + Top3 记忆 → 调模型
```

### 5. System 2：Consolidation（v0.5 新，懒计算 + 缓存混合方案）

MVP 不跑全局 Cron，按「用户触发 + 周报触发」双条件跑，冷启动成本低：
```
触发时机（满足任一条就跑当前用户）：
  · 用户首次 / 距上次超过 7 天时，进入【发现页】 → 立刻跑
  · 周报推送前（周日傍晚）且该用户近 30 天有活跃记录 → 主动跑

一次 Consolidation 流程：
  Step A. Episodic→Semantic 提炼：
    · 拉过去 7 天 events（过滤 deleted_at 非空）+ 已存 semantic_facts 全文
    · 一次 LLM Batch 调用输出 JSON：[{fact_type, content, confidence, source_event_ids}]
    · confidence ≥ 0.8 → INSERT semantic_facts(status='auto_confirmed')
      · 其中 0.8 ≤ confidence < 0.85 的条目自动入但额外标记 badge='low_confidence_auto'（发现页标徽章「低置信自动入」，用户可事后否认
    · 0.6 ≤ confidence < 0.8 → INSERT semantic_facts(status='pending') 推发现页 Tab2「关于我」待确认
    · 简单冲突检测：新 content 与已存 semantic_facts 语义相似度 ≥ 0.8 但方向相反 → status='conflict', conflict_with=旧条ID，推发现页冲突卡片
    · 写用户表 users.last_consolidated_at = now()
  Step B. Episodic→Procedural 提炼：
    · 过去 7 天 events.correction_log 非空条目 → 分类习惯偏好写 procedural_prefs（correction_habit 类型）
    · 行为信号扫描（events + ledger_actions 时间戳差算）：连续 3 次用户 10s 内接话 → reply_rhythm 调快 α=0.2 → delta≥0.2 下次聊天淡字告知，<0.2 默默生效
    · 每次调整写 procedural_prefs.adjustment_log + 写 ledger_actions
  Step C. 艾宾浩斯遗忘权重批量更新（对该用户 events 全量按 happened_at 算 weight 公式 UPDATE）
  Step D. 本次 Consolidation 所有动作写 ledger_actions 审计日志
```
未来（用户量 > 10w 后再考虑加全局每周日 03:00 Cron 批量处理活跃用户，MVP 暂不用。

### 6. AI 提取准确度（同 v0.3）
50 条真实样本，事件/备忘提取 ≥ 90%，情绪准确率 ≥ 80%。现在不做分类了，压力下降，目标不变。

### 7. 角色风格控制 + 智能自适应（同 v0.2，更新：值写进 procedural_prefs 表）
参数平滑插值 α=0.2，调幅 ≥ 20% 才告知用户，轨迹入 ledger_actions。

### 8. 备忘本地通知与系统日历联动（同 v0.4）
iOS UNUserNotificationCenter / Android AlarmManager，加备忘立刻注册，卸载重装从云盘同步 + 重新注册未到期。

### 9. 语音全链路（MVP 砍 TTS，只做 ASR + 录音 UI）
ASR 豆包/阿里云国内；生活声纹 SVG 用 Web Audio API 本地生成。

### 10. 共处状态机 + 视觉渲染（同 v0.4 升级 + v0.5 模式切换窗口约束）
状态机+Canvas/WebGL；**模式切换动作只能在呼吸的屏气/静息窗口触发**，避免呼吸和切换同时发生抢视觉注意力。

### 11. 生活页瀑布流性能（同 v0.4）
成熟组件 + 本地缩略图缓存 + 云端时 CDN 预取。

### 12. Policy 版本化与回放（v0.5 新增）
所有 Policy 决策写 ledger_actions.policy_version，可切版本做 A/B 测试。可回放某用户过去 7 天的全部 Ledger 动作复盘体验问题。

---

## 十四、待讨论的开放问题（v0.5 更新）

### v0.4→v0.5 已解决的（保留记录）
- ~~AI 分类准不准怎么办~~ → v0.5 决定：用户选三模式直接定分类，AI 绝不猜；仅提供分类弱提示不越界；事后改分类写进学习样本
- ~~三类石子要不要统一~~ → v0.5 决定：用户选分类，但数据在同一个 events 大表（加 stone_type 字段），记忆河可筛选可互转，真正做到"逻辑不统一但数据一层"

### v0.5 新开放问题→本次已收（保留决策记录）
11. **模式切换时机约束的体感** → **决策：放宽约束**。用户滑动立刻触发切换，不等呼吸窗；只是切换动画的起始帧节奏对齐到最近的呼吸阶段（屏气窗口内动画 1.2x 速，呼气窗口内 0.8x 速），体感上立刻动了但视觉节奏不打架。
12. **Episodic 永不删除的隐私边界** → **决策：加手动永久删除（二级确认+软删实现）**。操作：记忆河长按→删→两次确认弹窗「永久删除这条记录，AI 再也不会想起它。（无法撤销）」。技术：events 表加 `deleted_at` 字段，所有 System1 检索 / System2 巩固 / Views / 导出一律过滤 `deleted_at` 非空行，语义上彻底不存在；Ledger 保留删除动作日志可审计。另在【它页→隐私→设置】提供「清空一切记忆」三级确认入口。
13. **System 2 Consolidation 算力成本** → **决策：MVP 懒计算 + 缓存混合方案**。① 用户首次点【发现页】→ 立刻跑该用户过去 7 天 Consolidation → 结果入库 + 用户表写 `last_consolidated_at`；② 后续进发现页，距上次 Consolidation < 7 天直接读缓存，超过 7 天重跑；③ 周报推送时间（周日傍晚）→ 主动给近 30 天活跃用户跑 Consolidation 生成信件（不活跃先不跑，省成本）。
14. **Semantic 自动入置信度阈值** → **决策：MVP 先 0.8，低置信徽章，观察 50 用户后再调**。0.8 及以上自动入 Semantic；0.8-0.85 区间的事实自动入但在发现页标「低置信自动入」徽章（用户可事后否认）；0.6-0.8 仅推待确认池。上线后 2 周观察：自动入事实否认率 > 20% 则升到 0.83-0.85，< 5% 可降到 0.75。
15. **分类弱提示默认开关** → **决策：默认开 + 极低存在感 + 新手期限定**。细则：① 弱提示 opacity=0.25（比原方案更低更淡），不闪动，最小字号，5 秒自动消失；② 命中规则后仅 50% 概率弹出（进一步降打扰）；③ 累计输入满 30 次自动关闭弱提示（新手教学期结束）；④ 第一次弹出时带极小 × 按钮「不想看提示？关掉」；⑤ 用户可随时在【它页→它怎么对我】下手动再开/关。

### 仍开放的问题（保留 5 个旧问题未解决）
1. **倾诉输入的门槛**：用户每天愿意打多少字/说多少话？最低门槛一句话够不够？
2. **备忘通知的打扰度**：系统通知默认不响震动（静默）可以吗？还是默认响？
3. **记忆衰减的体感**：用户看到 3 个月前变模糊（不是"权重低了检索不到"，是视觉上真的淡），会有失去感吗？还是自然？
6. **品牌名与调性**："内在地形"仍为占位名，需尽快定（影响所有文案和用户招募）。
7. **首次引导的分寸**：首次打开除了光和淡 3 句模式引导，要不要再弹一个半透明的 3 句极简说明？还是完全不说？

---

## 文档状态

- 当前版本：v0.5
- 变更历史：
  - v0.1：初版（对话式 + 河流 + AI 周报）
  - v0.2：交互范式改为共处空间；新增智能自适应、语音对等、本地优先隐私架构、商业模式、目标用户
  - v0.3：隐私架构重写（透明+不留存+用户控制+加密同步）；商业模式暂缓
  - v0.4：5 Tab 页面架构；共处页全能输入+分类可见可修+修正即学习；备忘生活独立页规格；F2 风格初识分散问；记忆河降二级页；三类石子统一结构；10 开放问题
  - v0.5：共处页取消 AI 猜分类 → 用户手动三模式（📝/💭/📷）左右滑丝滑切换 + 弱提示；记忆架构对齐业界 2025 前沿 → 四层递进认知记忆（Working→Episodic→Semantic→Procedural）+ Raw Ledger/Derived Views/Policy 三段论 + System1/System2 双回路 + 每周睡眠巩固（Consolidation）+ 艾宾浩斯遗忘曲线权重 + 三重检索架构；Postgres 表设计精确定义；MVP 范围重写；技术关键点重写；15 开放问题（10 留 + 5 新）
- 下一步（建议顺序）：
  1. ~~讨论 15 个开放问题先收新 5 个~~ → ✅ 已决策（v0.5→本次）：切换时机放宽立刻触发+动画对齐呼吸；永久删除二级确认+软删；Consolidation 懒计算+缓存；Semantic 自动入阈值 0.8+低置信徽章；弱提示默认开+极低存在感+30 次后自动关
  2. 补【共处页三模式切换】高保真交互原型（HTML + Canvas），当前原型 app.html 只有旧版单输入框
  3. 定 LLM/ASR 第一供应商（国内优先：豆包/通义/DeepSeek/阿里云语音），做 50 条样本准确率测试
  4. 开后端工程：FastAPI + Postgres + pgvector，按本章表结构建表（events 含 deleted_at）+ System1 检索 Prompt 调优 + System2 Consolidation 懒计算逻辑
- 更新原则：每次重大讨论后更新版本号，保留历史版本
