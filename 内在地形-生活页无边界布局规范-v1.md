# 内在地形 · 生活页无边界布局规范 V1

> 适用范围：`app.html` 的 `生活` 页（信息流、单片展开态、留下一刻）。
> 上游权威：`内在地形-产品基线-v2.md` §5.4、§10。本文只规定**形式**，不改变任何记忆与权限语义。
> 状态：已定案，供实现直接照做。

---

## 0. 为什么要去掉卡片

今天每一片生活是一张玻璃卡：`border 1px` + `border-radius 18px` + `backdrop-filter blur(26px)` + `margin 0 20px`。

卡片是**发布单位**。一屏排开的圆角矩形，是社交时间线的语法：每一张都在说"这是一条内容，它等待被看见"。而生活页的定位是宣泄与记录，用户的唯一读者是智能体。用发布的形式装宣泄的内容，形式本身就在向用户要一个他并不想要的观众。

去掉边界后，一片生活不再是一张卡，而是**时间上的一段**。分组不再靠框，靠间距；分隔不再靠线，靠呼吸和时间刻度；智能体的回应不再靠一个套住它的小盒子，而是挂在时间轴上的一个节点。

本规范的主体是**删除**。新增的只有一条时间轴和一套间距。

---

## 1. 三条已定案的形式决策

| # | 决策 | 它解决什么 |
|---|---|---|
| 1 | **非对称栏**：媒体右出血到屏幕边缘，文字保留右边距 | 照片是世界的一部分，世界不在边距处停止；文字是说给人听的，需要边距才读得下去 |
| 2 | **五个间距值封闭**，不允许第六个 | 现状 11 个间距值互相打架，节奏是随机的。五个值让"什么和什么是一体"变成可读的规则 |
| 3 | **智能体的状态挂在时间轴上**，不挂在内容上 | 状态是时间性的（等待、到达、失败），不是内容的属性。挂在内容上就要给内容加框，框一加回来，无边界就废了 |

---

## 2. 五个间距值（封闭集）

```css
/* 生活页唯一允许的间距。任何 life-* / le-* / lap-* 规则里出现
   不属于这五个值的 margin / padding / gap，都是缺陷，不是风格选择。 */
--lf-tight:  4px;   /* 紧贴：同一个东西的内部 */
--lf-inner: 12px;   /* 片内：一片生活各部分之间 */
--lf-voice: 20px;   /* 换声：用户的话 ↔ 它的话；同时是页面左右基准内缩 */
--lf-piece: 44px;   /* 换片：两片生活之间 */
--lf-day:   88px;   /* 换日：两天之间 */
```

**为什么是这五个，而不是一条等比数列**：每个值对应一个语义边界，不是一个尺寸档位。取值只需满足一条判读律——

> `--lf-piece` (44) > `--lf-voice` (20) + `--lf-inner` (12)

即"一片生活连同它的回应"的内部最大间距（20），永远小于它与下一片的间距（44）。分组因此在任何内容组合下都不会读错，不需要任何边框来兜底。

**位置由间距叠加得出，不引入新常量：**

| 位置 | 计算 | 375px 屏上的值 |
|---|---|---|
| 时间轴 x | `--lf-voice` | 20px |
| 内容左缘（文字与媒体共用） | `--lf-voice + --lf-inner` | 32px |
| 文字右边距 | `--lf-voice` | 20px |
| 媒体右边距 | 0（出血） | 0 |
| 文字栏宽 | `100vw - 32 - 20` | 323px（≈21 汉字/行） |
| 媒体栏宽 | `100vw - 32` | 343px |

尺寸（图片高度、字号、命中区）不受五值约束——它们由比例和可用性下限决定。约束只针对**间距与内缩**。

**豁免（穷举，不可扩充）：**

1. 底部安全区 `var(--safe-bottom)` —— 设备给的，不是我们的节奏。
2. 1px 发丝线 —— 那是线，不是间距。
3. **一个控件自身的内边距**（药丸按钮、输入框、开关）—— 它决定的是命中区大小，属于「尺寸」而不是「节奏」，由 44px 最小命中区这条可用性下限管，不由五值管。判据：改这个值只会让这个控件变大变小，不会改变任何两个元素之间的距离。

第 3 条是真实存在的边界，写下来是为了让验收清单可判定；它不是给例外开口子——只要一个 padding 会影响**它之外**的任何元素的位置，它就受五值约束。

---

## 3. 时间轴

一条发丝线，`x = 20px`，从信息流顶端贯到末片底端——**包括月份耳语所在的那一段**。轴若在换月处断开，这一页就有了两条结构而不是一条。

```
颜色  rgba(255,255,255,.06)
宽度  1px
```

它不是装饰，是这一页唯一的结构。所有时间性的东西挂在它上面，所有内容性的东西挂在它右侧 12px 处。轴上只允许三类节点：

### 3.1 日刻度

出现在 `--lf-day` (88px) 的呼吸中间。轴上一段略亮的短线（长 12px，`rgba(255,255,255,.16)`），日期文字紧贴其右：

```
font: 400 10px/1 var(--serif-en); letter-spacing:.14em; text-transform:uppercase;
color: var(--life-green); opacity:.42;
例：TUE · JUL 28
```

### 3.2 时刻点

每一片生活的开始，轴上一个 3px 实心点（`rgba(255,255,255,.22)`）。时间不再写在片尾的 meta 行里，而是写在这个点的右侧、内容之前：

```
font: 400 9.5px/1 var(--serif-en); letter-spacing:.1em;
color: var(--text-faint);
例：21:04 · 窗台
```

> 这解决了一个旧毛病：`.le-meta` 把时间压在卡片底部，读的时候是"内容 → 它什么时候发生"。倒过来才是记忆的顺序——先落到时间里，再看见内容。

### 3.3 智能体节点（决策 3 的落点）

在这一片的内容结束后、`--lf-voice` (20px) 的呼吸之后，轴上出现一个节点。它有四种形态，且**只有节点形态在变，内容区永远没有框**：

| 状态 | 轴上形态 | 右侧内容 |
|---|---|---|
| 静默（`allow_response=false`） | **无节点** | 无。缺席就是正确的信号，不渲染"本片不回应"这类说明 |
| 等待（`response_pending`） | 5px 空心环，`1px solid rgba(var(--life-green-rgb),.34)`，呼吸 `opacity .3→.62 / 3.6s ease-in-out` | 无文字。不写"它正在想…"，不放 spinner，不放三个跳动的点 |
| 已回应 | 5px 实心点，`rgba(var(--life-green-rgb),.72)` | 回应正文，见 §3.4 |
| 失败（`response_failed`） | 5px 空心环断开为两段短弧（用 `border-color: … transparent`），色 `rgba(218,113,104,.42)`，不呼吸 | 一行实话 + `再试`，见 §3.5 |

呼吸动画在 `prefers-reduced-motion: reduce` 下降为静态的 `opacity:.48`。

### 3.4 回应的排版（无框）

回应与用户的话共用同一条左缘（32px），靠三件事区分，没有一件是边框：

1. 轴上的实心节点；
2. 一个极小的署名：`它` + `AI`，`9px var(--serif-en)`，`--text-faint`，其后 `--lf-tight` 接正文；
3. 正文 `300 13px/1.85 var(--serif-cn)`，`color: var(--text-dim)`——比用户的话（`15px/1.75`, `--text)` 更小更暗。它是回声，不是主体。

`来源`（回声引用的旧片）与`为什么现在`：默认折叠为一行 `--text-faint` 的 `9.5px serif-en` 提示，点开在原位展开，不弹层。来源展开后每条是"日期 + 一句摘录"，可点击滚动到那一片并让它的时刻点闪一次。

**删除**：`.le-agent` 的 `border`、`background` 渐变、`border-radius:14px`、`::after` 的 `agentGlow` 无限动画。发光是拟人化装饰，基线 §10.3 已禁止用存在感刷情绪。

### 3.5 失败态措辞

只允许这一句：

> 这一次它没能回应。**再试**

不写"服务异常""网络错误""稍后重试"。用户不需要知道是谁的错，只需要知道这不是他的错、以及下一步能做什么。`再试` 期间文案变为 `再试中…`，节点回到等待态的空心环。

---

## 4. 非对称栏与媒体构图（决策 1 的落点）

媒体的左缘与文字对齐（32px），右缘走到屏幕边缘（0）。栏内间隙统一 `--lf-tight` (4px)。

### 4.1 1–4 张：完整构图，全部可见

这个区间里，图片的数量本身是一种表达，一张都不能藏。

| 张数 | 构图 | 比例 |
|---|---|---|
| 1 | 单幅通栏 | **保留原始比例**，只夹在 `4:5` 与 `16:9` 之间。一张照片是一句完整的话，不许裁成方块 |
| 2 | 两等栏 | 每幅 `4:5`。成对时略竖，读起来是并置而不是拼接 |
| 3 | 左一高（跨两行）+ 右两叠 | 整体 `3:2`，左栏 `1.35fr` |
| 4 | 2 × 2 | 每格 `1:1` |

### 4.2 5–9 张：横向长卷，出血即是入口

超过四张就不再拼贴。改为**单行横向长卷**：高度统一为 `媒体栏宽 × 0.5`，每幅**保留自己的宽高比**，横向滚动，最后一幅被屏幕右缘切断。

这是右出血真正换来的东西：

- 被切断的那幅图，本身就是"还有更多"的告知。**不需要 `+5` 角标**——角标是在数量上讨论照片，长卷是在照片上讨论照片。
- 没有任何一张被藏在角标后面，全部一个手势可达。
- 不必为了填满方格而把竖图裁成正方形。九宫格会把一张竖构图切掉三分之一，长卷不会。

技术要点：`overflow-x:auto`、`scroll-snap-type:x proximity`、隐藏滚动条、`overscroll-behavior-x:contain`（避免与页面左右手势冲突）、右缘不加渐隐遮罩（遮罩是在替图片说话）。

**删除**：`.gi-n` 的"只显示 4 张"截断、`.gi-more` 的 `+N` 角标。

### 4.3 上限

单片视觉媒体上限 9（与后端 `MEDIA_LIMIT` 一致）。音频不占这 9 个位。第 10 张在留下一刻里就被拦住，不是保存后才报错。

### 4.4 语音

无框。轴右侧一行：播放键（32px 命中区）+ 波形 + 时长。波形是 20 根 `2px` 竖条、间隔 `--lf-tight`，**静止时不动**，只在播放时按进度点亮。空闲状态跳动的波形是假的音频可视化，它在假装机器听得懂声音。

有转写时，转写作为这一片的正文出现在语音行**之上**（与纯文字片完全同构）；无转写时只有语音行。转写旁不显示置信度数字（基线 §8.5），只在展开态的边界区说明"它可以听，但不会拿这段猜测去理解你"——对应后端未核对转写不得成为地形证据的规则。

---

## 5. 展开态

点任意一幅图，进入全屏单幅浏览。

- **背景纯黑**，不是模糊。模糊在说"后面还有界面在等你"；此刻应该只剩照片。
- 从被点击的那一幅**原位放大**（shared element），关闭时缩回原位。
- 一屏一幅，横向翻页；**原始比例完整显示，永不裁切**。
- 双指缩放；单指下拉关闭，跟手，图随位移缩小，松手判定阈值 `屏高 × 0.18`。
- 顶部只有 `3 / 9`（`10px serif-en`，`--text-faint`）与关闭。**没有分享、下载、点赞、更多**。这是私密记录，不存在"发出去"这个动作。
- 底部一行这一片的文字（单行截断，`--text-dim`），点一下展开全文。照片永不与用户当时说的话失联。
- Live Photo：长按播放动态部分。`LIVE` 标记只在静止时出现在左下角，`8px`，`opacity:.5`；播放时消失。

进入展开态时锁定背景滚动，退出后回到原滚动位置且被点的那一片的时刻点闪一次（`--life-green`，0.6s），用于重新定位。

---

## 6. 月份：从抽屉改为耳语

现状是可折叠的月份分组（`.life-month-header` + 箭头 + 计数 + `max-height` 过渡）。这是档案柜的隐喻，与产品原则 P1「形成，而不是归档」相反，且"这个月 23 片"的计数会把记录变成产量。

改为：滚动到某月区间时，屏幕顶部出现一行**不可点击**的低对比月份耳语（`11px serif-en`，`opacity:.34`，`position:sticky`），滚出即淡出。

**删除**：折叠箭头、月计数、`max-height` 动画、`toggleMonthGroup`。

需要按时间跳转的场景由既有的 `#lifeSearch` 承担，不由折叠承担。

---

## 7. 留下一刻（面板内的同一套规则）

面板保留为底部升起的卡（它是**动作**，动作有边界是对的；只有**记录**不该有边界）。但内部间距全部换成五值，并作两处修正：

1. 面板圆角由 `28px 28px 0 0` 改为 `20px 20px 0 0`，与 `--lf-voice` 同源，去掉一个孤立常量。
2. 图片预览格从 3 栏 `gap:8px` 改为 3 栏 `gap:--lf-tight`，与信息流中的媒体间隙一致——用户在面板里看到的构图节奏，就是他保存后会看到的节奏。

权限区（`只属于我` / `可成为地形证据` / `可被未来轻轻提起`）文案与层级不动。保存后只给一次轻反馈：新片的时刻点亮起并向下落位，不弹 toast、不计连续天数（基线 §5.4）。

---

## 8. 禁止清单

- 在信息流的任何内容上使用 `border`、`box-shadow`、`backdrop-filter` 或背景色块把内容围起来。
- 用 `+N` 角标代替可达的照片。
- 为单张照片强制统一裁切比例。
- 在空闲态播放假波形，或用 spinner / 跳动点表示"它在想"。
- 智能体回应带发光、脉冲光晕或任何"有生命"的装饰。
- 展开态出现分享、下载、点赞、收藏、热度。
- 显示片数、连续天数、月产量。
- 出现第六个间距值。

---

## 9. 验收清单

| # | 检查 | 判定 |
|---|---|---|
| 1 | 全页截图叠加网格 | 所有影响元素间距离的 margin/padding/gap 落在 `{4,12,20,44,88}`；例外只允许 §2 豁免的三类 |
| 2 | 信息流 CSS 审查 | `life-*` / `le-*` 规则中不存在 `border`、`border-radius`、`backdrop-filter`、内容背景色（时间轴发丝线与展开态遮罩除外） |
| 3 | 单片 + 回应 vs 相邻两片 | 前者内部最大间距 20 < 后者 44，无边框下分组判读无歧义 |
| 4 | 9 张图 | 横向长卷，最后一幅被右缘切断，无 `+N`，横向滚可达全部 9 张，每幅保留原比例 |
| 5 | 1 张竖图（3:4） | 不被裁成 16:10，按原比例通栏 |
| 6 | 文字右缘 vs 媒体右缘 | 文字距右 20px，媒体距右 0px |
| 7 | `response_pending` | 轴上空心环呼吸，内容区无任何占位文字与 spinner |
| 8 | `response_failed` | 轴上断弧 + 「这一次它没能回应。再试」，无红色告警块 |
| 9 | `allow_response=false` | 轴上无节点，也无任何"不回应"说明文字 |
| 10 | 展开态 | 纯黑底、原位放大、无分享/下载/点赞、下拉跟手关闭、底部带这一片的文字 |
| 11 | `prefers-reduced-motion` | 呼吸与落位动画降为静态，功能不缺失 |
| 12 | 月份 | 顶部耳语式 sticky，无折叠箭头、无计数 |
| 13 | 375px / 430px / 768px | 三档宽度下左缘 32px 恒定，文字栏 ≥ 20 汉字/行 |
| 14 | VoiceOver / TalkBack | 每片朗读顺序为「时间 → 地点 → 正文 → 它的回应」，时间轴节点不被朗读为可交互元素 |

---

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-08 | 首版。定案非对称栏（媒体右出血）、五值封闭间距、智能体状态挂时间轴；5–9 张图改横向长卷取代 `+N` 截断；月份折叠改耳语；展开态去社交动作 |
