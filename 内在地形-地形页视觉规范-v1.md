# 内在地形 · 地形页视觉规范 V1

> 适用范围：`app.html` 的 `地形` 页。
> 上游权威：`内在地形-产品基线-v2.md` §5.5、§8.5、§18.1；`habit_list_backend/docs/memory-v3-formation.md` §7。
> 姊妹文档：`内在地形-生活页无边界布局规范-v1.md`。本文沿用它的五个间距值，不新增第六个。
> 状态：已定案，供实现直接照做。

---

## 0. 这一页现在没有形状

今天的地形页是：一段宣言（`看见一个人正在形成什么`）+ 一条门槛说明（`至少 3 个独立时刻 · 跨 7 天 · 2 段情境`）+ 若干张玻璃卡 + 一段空态文案。

三个问题：

1. **它没有视觉资产。** 基线 §18.1 把「卡片、轻地图还是两者结合」列为待验证；那一条现在必须定案，因为这是产品唯一可被记住的画面。宣言不是画面，宣言是说明书。
2. **它把门槛当页面家具。** 常驻的 `3 / 7 / 2` 把这一页变成进度条。用户一旦看见门槛就会去凑门槛，而 P3 说「不形成记忆是正常结果」。门槛应该只在**一块地貌浮现时**作为「为什么现在出现」讲一次，不该天天挂着。
3. **空态是常态，但空态被当成异常处理。** 前十四天大多数用户看到的就是空态。空态是这一页的主要形态，它必须本身是好看的，而不是一句安慰。

---

## 1. 定案：不是卡片，也不是地图，是**剖面**

基线 §18.1 的三选一，答案是第四个。

### 1.1 为什么不是轻地图

地图需要坐标。而我们的数据里**没有任何一个诚实的空间量**：两块地貌之间没有距离，没有方位，没有相邻关系。画一张地图就得把这些编出来，编出来的邻接会被用户读成洞察——那正是 P4「证据先于结论」禁止的东西。

而且地图有留白，留白会被读成「还没填满」。这会把克制型产品变成收集游戏，与 P6 相反。

### 1.2 为什么不是卡片

卡片没有错，但卡片没有形状。一列圆角矩形是所有 App 的语法，它无法回答「内在地形长什么样」。用户说「缺乏特色」，缺的就是这个。

### 1.3 定案：**地层剖面**

数据里真实存在、且唯一真实存在的连续量是**时间**：每块地貌都有 `first_seen_at → last_seen_at`，有一个状态，有若干个可追溯时刻。

所以这一页的形状是：

> **一条横向的时间轴，若干条横跨其上的带。**
> 带的左右两端 = 这块地貌真实的起止时间。带的样子 = 它的状态。带的多少 = 你身上正在发生的事情的多少。

没有一个像素是编的。

### 1.4 与生活页同源

生活页是**一条竖轴 + 挂在轴上的时刻**。地形页是**一条横轴 + 跨在轴上的带**。

同一套语法旋转 90°：轴是唯一的结构，内容挂在轴上，状态挂在轴上，边界靠间距不靠框。

两页因此是同一个产品的两个视图，而不是两个产品。这是「艺术感」的来源——不是多加装饰，是同一条规则贯穿到底。

---

## 2. 剖面的构造

```
                        ← 更早                      现在 →
  ┌─────────────────────────────────────────────────────────┐
  │                                          ╭──╮           │  ← 此刻天气（无跨度，贴在「现在」）
  │        ▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▂▂▂▃▃▃▃▃▃▃▃▃▃▃              │  ← 正在长出来（右端未闭合）
  │   ▃▃▃    ▃▃▃      ▃▃▃         ▃▃▃    ▃▃▃                │  ← 反复回到（断续的节拍）
  │  ▃▃▃▃▃▃▃▃▃▃▃▃▃▃▂▂▂▁▁▁ ░ ░  ░                            │  ← 正在松动（右端散开）
  │  ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔  │  ← 地平线（轴）
  └─────────────────────────────────────────────────────────┘
     JUL                        AUG
```

### 2.1 轴

一条发丝线，`rgba(255,255,255,.08)`，`1px`，横贯整个剖面区，左右各内缩 `--lf-voice`。

轴上只有两类刻度：

- **月刻度**：`10px var(--serif-en)`，`letter-spacing:.14em`，uppercase，`opacity:.34`。落在轴下方 `--lf-tight`。
- **现在**：轴的右端一个 `3px` 实心点，`--river-rose`，`opacity:.7`。它是唯一有颜色的刻度，因为「现在」是唯一一个用户站在里面的时刻。

轴不滚动分页。时间范围由**全部可见地貌的并集**决定，即 `min(first_seen_at)` 到 `now`，整体等比压进一屏宽。范围随数据自然变长，不做缩放手势——这一页不是可导航的地图（§1.1）。

### 2.2 带

一条带的几何：

| 属性 | 取值 | 依据 |
|---|---|---|
| 左端 x | `first_seen_at` 在轴上的位置 | 真实数据 |
| 右端 x | `last_seen_at` 在轴上的位置 | 真实数据 |
| 最小宽度 | `24px` | 单日跨度也要可点（命中区下限，不是节奏） |
| 高度 | `6px` | 常量。**不由证据数决定** |
| 竖向顺序 | 按 `first_seen_at` 升序，早的在下 | 「早的在下」是地层的方向；这是事实排序，不是重要性排序 |
| 泳道高度 | `44px`，带在其中竖向居中 | 带只有 6px，够不上命中区下限。泳道是**尺寸**（命中区），带之间因此不需要任何 margin / gap |

**高度是常量，这是一条硬规则。** 让高度随证据数变化，就是在画置信度柱状图，违反 §8.5「界面不直接展示模型小数置信度」，也违反 §5.5「不展示内容数量」。**一块地貌不比另一块更真。**

### 2.3 时刻点

每条带上，在每个证据发生的时间位置画一个 `2px` 的点，`rgba(255,255,255,.42)`。

这是这一页唯一暴露「有多少条证据」的地方，而且它是**位置性**的而不是**计数性**的：用户看到的是「这件事在这些时候发生过」，不是「这件事得了 5 分」。点密的地方读起来是「那段时间它反复出现」——这正是我们希望被看见的东西。

不写数字。不做 tooltip 计数。

---

## 3. 五种地貌的形状语言

**颜色不承担分类。** 五种地貌全部用同一个色相（`--river-rose`），靠**形状**区分。理由：颜色分类需要图例，图例是说明书；形状可以被直接读出来。

| 地貌 | 形状 | 为什么是这个形状 |
|---|---|---|
| **正在长出来** | 左端收细、向右渐宽，右端**不闭合**（渐变到透明） | 它还在发生，右边不该有一堵墙 |
| **反复回到** | 等高的断续段，段间留空 | 它不是持续的，它是回来了很多次 |
| **正在松动** | 左端实、向右逐渐散成点。左端的「实」必须真的是实的：颗粒只从右侧长出来 | 曾经牢固，现在边界不清了。若整条都是均匀虚线，它会与「反复回到」读成同一个形状 |
| **两股力量** | **一条带分成上下两股**，各 `3px`，中间 `1px` 空隙；两股在时间上互相错位 | 两侧都真实存在（§8.4）。不画成对抗的箭头，画成同时存在的两层 |
| **尚未命名** | 一条虚线轮廓的带，**内部空**，无文字 | 有形状但还没有名字。空心就是「还说不清」，不需要文案解释 |
| （消退） | 整条带 `opacity:.28`，仍在轴上，不移除 | 沉寂不是删除（§8.7）。它还在你的地层里 |

> 消退的淡化落在**泳道**上，不落在带上。两个理由：时刻点是泳道的子元素，落在泳道上它们才跟着一起淡（「整条带」包括它的时刻点）；而且泳道级的淡化与 §6「其余带降到 `.34`」是**相乘**的，落在带上则会被后者按优先级覆盖——那会让「点开另一块地貌」把一块沉寂的地貌照亮，正好说反。

`prefers-reduced-motion: reduce` 下所有带静态呈现；默认状态下**只有一种动效**：新浮现的带在首次进入视口时从左端向右画出一次（`700ms`，`cubic-bezier(.2,.8,.2,1)`），之后永不再动。带不呼吸、不发光、不脉冲——那些是拟人化装饰（§10.3）。

---

## 4. 此刻天气：轴的右端

基线 §5.5 把「此刻天气」列为首项，今天页面上根本没有它。

它不是一条带，因为它**没有跨度**——它只属于现在。所以它贴在轴的右端「现在」那个点的上方，是一小片 `12px` 的柔光晕（`radial-gradient`，`--comp-warm`，`opacity:.5`），旁边一行 `11px var(--serif-cn)` 的短语。

它必须看起来是**会散掉的**：比任何一条带都轻、都软、都没有边界。用户应当在不读任何文案的情况下就知道这个东西不稳定。

不显示情绪评分、不显示表情符号、不做七日天气曲线（那是情绪统计，§14 暂不做清单）。天气只有当下一个词。

### 4.1 那个词是回声，不是推断

数据源是 `app/memory/weather.py`，它只把用户**自己写下的那个词**取出来（「今天好累」→ 累），不调模型、不换更漂亮的说法。读不出来就是没有天气，槽位保持空——空槽位是正确结果，不是失败。理由：一旦允许它造词，这一缕就变成了「它替你定义你此刻是谁」。危机与敏感话轮一律不读，把一个正在说不想活的人做成一朵柔光是把痛苦当装饰。

### 4.2 天气不累积成序列

原设想的「可积累的天气序列」被**否决**，三条上游各自单独就足以否决它：基线 §11 把此刻天气归在 Working 层并要求「限制长期化」；一条可累积的序列就是本规范 §7 与基线 §14 都拒绝的情绪统计；而拿推断去喂形成层违反 P4「证据先于结论」——地形会长在模型的猜测上，而不是长在用户真的说过的话上。

所以天气写在 `Working.mood` 上，随会话过期（`TERRAIN_WEATHER_TTL_HOURS`，默认 12 小时），永不进 `MemoryClaim`、永不是地形证据。会累积的是天气**底下**那句原话，它本来就在走证据通路。接口里连表达序列的形状都不存在：`GET /terrain` 的 `weather` 是对象或 `null`，没有 `weather_history`。

### 4.3 它是页面上唯一「点一下就消失」的元素

因为它本来就是会散掉的东西。点击 → `DELETE /terrain/weather` → 520ms 的 `blur + fade` 后移除（减动效下直接落到终态，不缺功能）。散掉的只是**这次读法**：只清 `mood`，用户说过的那句话一个字都不动。`?mute=true` 则彻底停止读天气。暂停记忆的人也不读——地形页上多出一个状态词，仍然是产品在对他下判断。

---

## 5. 空态就是地平线

没有任何地貌时，页面上留下：**一条完整的轴、月刻度、现在点、此刻天气。**

也就是说——**空态不是别的界面，空态就是剖面本身，只是上面还没有起伏。**

文案两行，压在轴的下方，左对齐到 `--lf-content-x`：

> 这里还没有起伏。
> 我们可以只相处，不急着得出结论。

第二句直接取自基线 §5.5 的诚实空态。**不放插画、不放示例地貌、不放「距离第一块地形还差 N 个时刻」。** 最后这个尤其要拒绝：它是把门槛变成任务条，恰好是 §0 第 2 条的问题。

---

## 6. 从带到地貌卡

点一条带，它**在原位向下展开**成完整的地貌卡，剖面其余部分下移。不弹层、不跳页——用户不该失去自己在时间里的位置。

展开时该带保持高亮（`opacity:1`，其余带降到 `.34`），轴上同时出现这块地貌的时间范围括线。

卡片内容严格按基线 §8.5 契约，顺序不可改：

1. **一句形成性表达**（`300 16px/1.85 var(--serif-cn)`）。用户共同命名过，就用用户的词，并在其后极小地标注原表达可展开。
2. **状态**（`10px`，`letter-spacing:.18em`）。文字取五种地貌名，不取 `forming/strengthening` 这类内部枚举。
3. **时间范围**（`10px var(--serif-en)`）。
4. **为什么现在出现**：直接用后端 `why_now`。**门槛只在这里出现一次**——「它在 3 段不同情境里出现过，跨了 21 天」是解释，不是家具。
5. **证据摘要**：默认折叠为一行提示，点开在原位展开；敏感原文默认再折一层（§8.5）。
6. **校正**：`像我 / 不像我 / 暂时不知道` —— 三个等重的药丸，横排。
7. **控制**：收在一个 `⋯` 后面（修改表达 / 共同命名 / 隐藏 / 禁止主动引用 / 永久删除）。

### 6.1 七个按钮必须收起来

今天一张卡上并排七个按钮。那是控制面板的样子，而这一刻应该是「认出自己」的一刻。

规则：**校正是决定，露在外面；控制是管理，收进 `⋯`。** 除了一个例外——`共同命名` 同时也是签名动作（基线 §9 P0），当一块地貌已被确认为「像我」且尚未命名时，`共同命名` 从 `⋯` 里升到外面，替换掉已经用过的「像我」的位置。

`永久删除` 永远在 `⋯` 内，且需二次确认。

### 6.2 共同命名：签名动作长什么样

这是整个产品的签名动作——用户拿自己的词，接替模型的说法。所以它不能长成 `window.prompt`：那是浏览器的字体、按钮和语言，是这一页唯一一处会突然变成「别人的产品」的地方。

**表单开在这块地貌自己的卡里**，位置就是第 6 项校正区，§8.5 的七项顺序因此不变。命名进行中：

- 那句形成性表达降到 `--text-faint`——用户的词正在接替它，这一步在视觉上就是交接。
- 校正行与 `⋯` 菜单让位（`display:none`）。这一刻页面上只剩一个决定。
- 输入框唯一的边界是一条 `--river-rose` 的下发线（`border-bottom`），其余全部让给用户正在写的那个词。上限 24 字：这是一个**名字**，不是一句重述。
- 两个按钮：`就叫这个`（primary）/ `先不`。`Enter` 提交，`Esc` 退出，空值不提交。

**模型的说法只是种子，不是选项。** 点一下把它填进输入框，用户还得自己按下 `就叫这个`。若做成一按即定，「共同命名」就退化成「替它按确认」。装不进输入框的说法（后端 `name_options` 里 `claim_text` 可长至 48 字）**不做种子**——它已经完整地写在上面那行表达里，用户读得到；塞进来只会诱导一个存不下的名字，或者被无声截断。

改名走同一个表单，从 `⋯` 进入，输入框预填现有的名字。

---

## 7. 禁止清单

- 常驻门槛条、进度条、「还差 N 个时刻」。
- 用带的高度、粗细、亮度表达证据数量或置信度。
- 用颜色区分五种地貌（颜色不分类，形状分类）。
- 任何空间隐喻中编造的邻接、距离、方位、海拔。
- 空态里出现示例地貌、插画或占位卡。
- 情绪评分、七日情绪曲线、表情符号天气。
- 带发光、呼吸、脉冲；带上出现无限循环动画。
- 弹层展示地貌卡（必须原位展开）。
- 一张卡上并排超过四个同等级按钮。
- 出现第六个间距值。

---

## 8. 间距

沿用生活页的封闭五值（`4 / 12 / 20 / 44 / 88`）与 `--lf-content-x`。地形页新增的只有两个**尺寸**（不是节奏，不受五值约束）：

```css
--tr-band-h:  6px;   /* 带高。常量，见 §2.2 */
--tr-band-min:24px;  /* 带的最小可点宽度 */
--tr-lane-h: 44px;   /* 泳道高＝命中区下限 */
```

判据同生活页 §2 豁免第 3 条：改这三个值只会让带自身变粗、变长、命中区变高，不改变任何两个元素之间被声明的距离——地形页的带之间没有 margin、没有 gap。

---

## 9. 验收清单

| # | 检查 | 判定 |
|---|---|---|
| 1 | 0 块地貌 | 轴、月刻度、现在点全部在位；两行诚实文案；无插画、无示例卡、无进度提示。天气槽位在位但无数据时不出现（§4） |
| 2 | 1 块地貌 | 带的左右端与 `first_seen_at / last_seen_at` 在轴上的位置一致（误差 ≤ 1px） |
| 3 | 2 块证据数差 5 倍的地貌 | 两条带**等高**；页面上无任何数字表达证据多少 |
| 4 | 单日跨度地貌 | 带宽 ≥ 24px 且可点 |
| 5 | 五种地貌各一 | 仅凭形状可区分；页面上无图例；五者色相相同 |
| 6 | 消退地貌 | 仍在轴上，`opacity` 明显更低，未被移除 |
| 7 | `two_forces` | 渲染为上下两股，不是一条 |
| 8 | `unnamed` | 空心虚线带，无文字，点开后有共同命名入口 |
| 9 | 点带 | 原位展开，不弹层；其余带降到 `.34`；页面滚动位置不跳 |
| 10 | 展开的卡 | 七项按 §6 顺序齐备；外露按钮 ≤ 4；删除在 `⋯` 内且二次确认 |
| 11 | 门槛文案 | 全页只出现一次，且只在展开卡的「为什么现在出现」里 |
| 12 | 已共同命名的地貌 | 卡上主表达是用户自己的词 |
| 13 | 间距审计 | 影响元素间距离的 margin/padding/gap 全部落在 `{4,12,20,44,88}` |
| 14 | `prefers-reduced-motion` | 带不画出、无动效，信息不缺失 |
| 15 | 375 / 430 / 768px | 轴左右内缩恒为 20px；带不溢出；月刻度不重叠 |
| 16 | VoiceOver / TalkBack | 每条带朗读为「地貌名 · 状态 · 时间范围」，可激活；轴与时刻点不被朗读 |
| 17 | 此刻天气 | 有词时出现在轴右端上方（右内缩 20px，命中区 ≥ 44px）；`null` / 空串 / 缺字段一律隐藏；点一下散掉后不再出现 |
| 18 | 天气不是序列 | `GET /terrain` 的 `weather` 是对象或 `null`，无 `weather_history`；散掉只清 `mood`，原话仍在；暂停记忆或 `mute` 后读不到词 |
| 19 | 共同命名 | 表单开在卡内不弹层；页面上无 `window.prompt`；命名中校正行与 `⋯` 让位；`Enter` 提交、`Esc` 退出、空值不提交 |
| 20 | 命名种子 | 点种子只填进输入框，不直接提交；超过输入框上限的说法不出现在种子里，也不被截断 |

---

## 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-08-08 | 首版。定案基线 §18.1：地形页形态为**地层剖面**（横向时间轴 + 带），否决轻地图与纯卡片；带高恒定以拒绝把证据数画成柱状图；颜色不分类、形状分类；此刻天气落在轴的右端；空态即剖面本身；地貌卡改为原位展开并把控制项收进 `⋯`；常驻门槛条删除 |
| v1.1 | 2026-08-08 | 此刻天气接上数据源（`app/memory/weather.py`）：**回声用户自己的词，不推断**。否决「可积累的天气序列」——见 §4.2 三条上游各自单独即可否决。补 §4.1/4.2/4.3 与验收 #17/#18 |
| v1.2 | 2026-08-08 | 共同命名从 `window.prompt` 改为卡内表单（§6.2）：模型的说法只是**种子**不是选项；命名中表达降为 faint、校正行让位。补验收 #19/#20 |
