# 内在地形 · 生活碎片互动实施任务书

> **历史交付任务书（非当前产品契约）**：当前实现和验收以 [`内在地形-产品基线-v2.md`](内在地形-产品基线-v2.md) v2.3 为准。本文件中的“不留痕”、旧意图字段和延期语句只用于识别迁移兼容面；新客户端不得重新暴露这些概念。

> 版本：v1.0  
> 阶段：Alpha 核心闭环打磨  
> 日期：2026-08-03  
> 实施对象：后续负责编码的智能体  
> 质检对象：独立质检智能体

## 1. 任务目标

把“生活碎片”做成内在地形最有辨识度的产品入口：用户可以记录真实生活，智能体不会机械地给每条内容点赞或评论，而是根据内容、关系和用户偏好，选择安静看见、短暂停留或在合适时机产生带来源的回声。

本阶段的成功不是让智能体说得更多，而是让用户感到：

> 它知道什么时候应该回应，也知道什么时候不打扰；过了一段时间，它能把现在和过去轻轻连起来，但解释权仍然在我手里。

## 2. 现有产品边界（必须保留）

以下决策已经冻结，实施不得擅自恢复或替换：

- 一级入口保持 `共处 / 生活 / 地形 / 我们`。
- 生活碎片是用户主动记录的私密生活流，不是公开动态或社交广场。
- 共处不会自动生成待办。
- 共处对话不会自动进入记忆河流或地形证据。
- 生活碎片默认不进入地形；只有用户明确允许时才可作为候选来源。
- 碎片中的 AI 回应只属于当前碎片线程，不生成待办、地形证据或共处记忆。
- 默认回应偏好为“偶尔回应”；“每条回应”必须由用户主动开启。
- 不做点赞数、评论数、连续签到、积分、排行榜或公开互动数据。
- 回声只在 App 内出现，本阶段不做系统推送。
- 语音输入、语音回答、图片理解、完整管理员控制面和正式部署不属于本任务。

## 3. 核心闭环

```text
用户记录一刻
  -> 记录先保存，不要求用户整理
  -> Agent 判断是否值得回应
  -> 安静看见 / 短暂停留 / 带来源回声
  -> 用户可以回一句，继续当前碎片线程
  -> 过一段时间，允许的旧片段才可能与现在形成回声
  -> 用户确认、否认、隐藏或禁止某类回应
  -> 后续回应策略真实改变
```

绝大多数碎片可以只保存、不回应、不进入长期形成。这是正常结果，不是失败状态。

## 4. 互动模型

### 4.1 用户可见的三种回应

| 类型 | 用户感受 | 允许的内容 | 禁止的内容 |
|---|---|---|---|
| 看见 | “它收到了，但没有打扰我” | 极短的存在确认，或只显示轻反应 | 空泛夸奖、点赞数、情绪绑架 |
| 停留 | “它被某个具体细节留住了” | 一句具体回应，最多一个轻问题 | 总结人格、建议清单、自动任务 |
| 回声 | “现在和过去有一处真实连接” | 明确说明连接点，并展示来源 | 为炫耀记忆而翻旧事、引用敏感来源 |

内部枚举可以继续使用 `seen / paused / echo`，但用户界面应使用自然语言，不暴露模型术语。

### 4.2 回应决策输入

Agent 可以使用以下信号做“是否回应”的判断，但这些信号只用于当前策略，不得直接变成用户事实或地形结论：

- 内容是否包含具体生活细节；
- 是否和近期碎片重复；
- 是否出现新的变化、关系或选择；
- 当前用户的回应偏好；
- 最近一段时间的智能体回应密度；
- 当前碎片是否允许主动回应和回声；
- 是否属于敏感、危机、不留痕或不应主动引用的场景。

不要把“自动分类”展示成用户必须选择的标签。分类只负责路由，不负责定义用户。

### 4.3 回应硬规则

- `silent`：新碎片不创建自动回应事件；用户主动回一句后可以正常回复。
- `occasional`：允许返回“不回应”；普通或信息薄的碎片应多数保持安静。
- `always`：每条新碎片都要有回应，但仍可只有轻反应或一句短评。
- 用户主动在碎片线程回一句时，必须优先回复当前线程，不触发其他记忆链路。
- 评论最多两句，尽量少于 80 个中文字符，最多一个轻问题。
- 必须引用当前片段的具体细节；禁止“感谢分享”“继续加油”“为你点赞”等套话。
- 禁止“你总是”“你从来就是”“这说明你是……”等固定人格判断。
- 危机文本走安全回应，不能进入地形、普通回声或主动召回。

### 4.4 频率预算

- 默认偶尔回应：至少保留一个“可以安静”的结果；第一版可沿用现有的低频预算，但不要只依赖简单计数，后续应加入重复、时机和敏感度判断。
- 主动回声：每个 App 会话最多一条；如果现有系统没有会话边界，可用 24 小时窗口替代。
- 同一来源不应在短期内反复被引用。
- 用户明确说“别再这样提”后，相关来源或来源类别必须进入抑制列表，直到用户主动恢复。

## 5. 回声 V1

### 5.1 出现位置

第一版只允许在以下 App 内场景出现：

- 用户打开生活页时，作为一条低频提示；
- 用户回到共处时，在当前意图允许的情况下出现；
- 用户打开某条生活碎片线程时，显示与当前片段有关的历史来源。

不要在系统通知、锁屏、后台或用户离开产品时主动召回。

### 5.2 回声资格

同时满足以下条件才可以生成回声：

- 当前内容与旧片段有明确具体的连接点；
- 旧片段仍为 active，且用户允许主动引用；
- 当前会话不是不留痕、危机或明确的敏感场景；
- 没有超过回应频率预算；
- 回声能帮助用户看见连续性或变化，而不是单纯证明“系统记得”。

每条回声必须能回答：“为什么现在提起？”并提供来源查看、关闭本条和禁止类似回声的入口。

### 5.3 来源与删除

- 回声必须携带来源 ID，不允许只有无法追溯的摘要。
- 来源被隐藏、删除或撤销授权后，不得继续展示。
- 删除来源时，应取消尚未处理的相关 Outbox，并阻止旧事件重新生成回应。
- 不得把 AI 生成的回应当作回声来源或地形证据。

## 6. 用户反馈闭环

至少支持以下反馈，并让反馈真实影响后续行为：

| 反馈 | 后续行为 |
|---|---|
| 这不像我 | 降低该类表达或关联的权重，停止主动重复使用 |
| 少这样回应 | 降低回应密度或对应风格 |
| 别再提这条 | 立即抑制该来源 |
| 别再提这类生活 | 抑制对应来源类别或主题，不能只记录 Toast |
| 暂时不知道 | 保持未定，不强行转成确认结论 |
| 修改表达 | 以后以用户修改版本为当前表达，旧版本保留可审计关系 |

优先复用现有用户事件、设置和记忆控制模式。只有在查询、审计或删除语义确实需要时，才新增独立表，不要为每个反馈随意创建新的记忆对象。

## 7. 隐私、删除与失败语义

- 保存生活碎片必须先成功，AI 生成失败不能导致记录丢失。
- Outbox 只传递必要的标识和策略参数，不在队列中复制完整私密正文。
- Worker 必须幂等：重复消费不能产生重复回应。
- 碎片删除后，关联互动、待处理事件和回声候选都要遵守删除状态。
- 删除后不能因旧 Outbox、缓存、重试或来源检索而复活。
- 模型超时、限流或结构化输出失败时，界面显示可理解的待处理或失败状态，不伪造成功。
- 管理员默认不能搜索或浏览用户完整私密文本。

## 8. 本阶段实施范围

### 必须完成

1. 生活碎片回应资格与低频策略从简单计数升级为可解释的策略门。
2. 回声 V1 的 App 内展示、来源、关闭和权限控制。
3. 用户反馈真实影响回应和回声。
4. 删除、隐藏、撤销授权与 Outbox/缓存的完整闭环。
5. 结构化模型输出、超时、重试、幂等和日志追踪。
6. API、Worker、迁移和手机尺寸 UI 的自动化验证。

### 明确不做

- 语音输入或语音回答；
- 系统推送；
- 公开社交、点赞数、关注、评论数；
- 自动待办、自动日历和自动提醒；
- 每次对话自动进入长期记忆；
- 固定周报、情绪分数、连续签到和积分；
- 完整管理员端和正式生产部署。

## 9. 实施约束

- 开工前必须阅读 `内在地形-产品基线-v2.md`，并检查当前 dirty worktree；不得重置或覆盖已有用户改动。
- 优先复用现有 API、Outbox、Memory V2、权限、删除和日志模式。
- Prompt 必须有版本号，模型输出必须有结构化 schema；不接受依赖自由文本解析的关键分支。
- 所有异步行为必须带 request ID、策略版本和可追踪的来源信息。
- 新增行为应先补单元或集成测试，再修改前端展示。
- 本轮只做本地验证，不启动手机测试环境、不部署服务器。
- 产品基线发生变化时，先更新产品决策或 ADR，再改代码。

## 10. 验收矩阵

| 场景 | 预期结果 |
|---|---|
| 普通碎片 + 默认偶尔回应 | 可以安静，也可以短回应；不创建待办、地形或共处记忆 |
| 安静看着 | 不创建新自动回应 Outbox；用户主动回一句仍可得到回复 |
| 每条回应 | 每条有回应，但不出现任务、诊断或泛化套话 |
| 用户回复碎片 | 回复留在当前碎片线程，不污染其他链路 |
| 未允许回声 | 不返回 source ID，不引用旧片段 |
| 允许回声但无强关联 | 保持安静，不为了填满页面硬生成回声 |
| 有效回声 | 解释为什么现在出现，展示来源，可关闭和禁止类似回声 |
| 用户说“别再提” | 当前来源或类别立即被抑制，后续检索不会再次主动引用 |
| 删除碎片 | 关联回应、待处理事件和来源引用不可复活 |
| 不留痕共处 | 不进入地形、回声、普通记忆或待办 |
| 危机表达 | 使用安全回应，且不进入长期形成和主动回声 |
| 模型超时/失败 | 碎片仍保存；不伪造 AI 回应；重试不重复写入 |
| 重复消费同一 Outbox | 最多生成一条对应回应 |
| 手机 390x844 | 底部导航、碎片线程、回声来源和反馈入口不重叠、不溢出 |

## 11. 交付物

实施智能体交付时必须提供：

- 改动文件清单和每个文件的目的；
- 数据库迁移、升级与回退结果；
- API、Worker、模型和 Prompt 版本变更说明；
- 自动化测试命令、结果和未覆盖项；
- 关键手机尺寸截图或本地浏览器验收说明；
- 已知风险、降级行为和未完成事项；
- 不得声称“已上线”或“手机已更新”，除非用户另行授权。

建议交接格式：

```text
目标：
已完成：
改动文件：
数据库迁移：
测试命令与结果：
手工验收场景：
已知限制：
需要质检重点：
```

## 12. 独立质检标准

质检不以“代码能跑”作为通过条件，而按以下顺序进行：

1. 对照本任务书和产品基线做行为审查；
2. 检查记忆边界、来源权限、删除复活、Outbox 幂等和安全分流；
3. 审查关键代码、迁移、Prompt schema、日志和异常路径；
4. 运行单元、集成、迁移、Worker 和前端手机尺寸测试；
5. 验收安静结果、空状态、失败状态和低频时机；
6. 检查体验是否仍像私密生活被见证，而不是社交动态或 AI 评论机器人；
7. 按严重程度输出问题，先列阻塞项，再列风险和残余测试缺口。

只有在以下条件同时满足时，才建议进入下一阶段：

- 生活碎片互动不污染待办、地形或共处记忆；
- 回声有来源、有权限、有频率限制且可关闭；
- 用户反馈能改变后续行为；
- 删除后没有复活路径；
- 危机和敏感内容不会被当作普通洞察；
- 关键测试和手机尺寸验收通过；
- 产品体验仍然克制、有辨识度。
