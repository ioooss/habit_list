# ---------- 部署连接（只使用 SSH Key，不支持在 env 保存 SSH 密码）----------
SERVER_HOST=your_server_ip
SERVER_USER=ubuntu

# 后端开发配置见 habit_list_backend/.env.example；生产配置见
# habit_list_backend/.env.production.example。真实密钥只进入对应的忽略文件。
