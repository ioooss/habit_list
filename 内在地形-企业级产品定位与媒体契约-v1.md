# 内在地形：企业级产品定位与媒体契约 v1

## 一句话定位

内在地形是一款以用户主权为前提的 AI 陪伴型生活记录产品：它保存真实生活，低频地回应被明确允许的片段，并在跨时间证据足够时帮助用户看见“一个人正在形成什么”。

它不是自动记账的聊天机器人，不是待办生成器，不是心理诊断工具，也不是把每次对话堆成时间线的日志库。

## 产品价值

1. **真实留下**：文字、原始语音、普通图片和 Live Photo 可以自由组合。一段语音的原文件是事实本身，转写只是可选元数据。
2. **有分寸地回应**：生活碎片默认先被保存。AI 只在策略允许、内容安全且能说明来源时回应；回应属于原碎片线程，不会暗中生成待办或共处记忆。
3. **看见形成中的地形**：地形只由跨时间、跨情境、可追溯的证据形成。用户能命名、增强、松动、冲突或删除它，模型不能替用户下定论。
4. **反馈真的改变行为**：“这不像我”“别再提”会写入可审计的策略规则，并影响之后的回应、回声候选和地形检索。

## 四个产品空间

| 空间 | 用户任务 | 智能体边界 |
| --- | --- | --- |
| 共处 | 在当下说话、发语音、听回答 | 处理当前会话；不把普通对话自动变成生活碎片或待办 |
| 生活 | 留下一片具体生活 | 保存原始内容；只按明确权限参与回应、回声或地形 |
| 地形 | 查看正在形成的模式 | 展示证据和变化，不展示黑箱人格标签 |
| 我们 | 管理相处方式和数据边界 | 展示当前策略、反馈影响、媒体契约和权限状态 |

## 媒体契约

### 支持的输入组合

- 纯文字
- 纯原始语音
- 纯图片
- 纯 Live Photo
- 文字 + 原始语音
- 文字 + 图片 / Live Photo
- 原始语音 + 图片 / Live Photo
- 文字 + 原始语音 + 图片 / Live Photo

每片生活最多 **9 个可见媒体内容**。Live Photo 的静态图和动态视频属于同一个媒体组，只计 1 个；传输层允许 18 个原始资产，以容纳 9 组 Live Photo。

### 原始性与转写

- 原始音频文件永远保留并可回放；它是生活记录的权威来源。
- ASR 只能作为用户主动触发的可选元数据，失败不能阻止保存或删除原音频。
- 共处和生活线程都可以发送原始语音；需要文字时再生成转写，文字可单独编辑。

### Live Photo 组

- 静态部分：`image/heic` 或 `image/heif`，角色 `live_still`。
- 动态部分：`video/quicktime`、`video/mp4` 或 `video/webm`，角色 `live_motion`。
- 两个原始资产共享显式 `media_group_id`，不能依赖文件名作为持久关系。
- 删除、权限撤回、生命周期清理均以组为单位，不能遗留另一半资产。

## 企业级质量门槛

- **数据闭环**：上传、绑定、播放、删除、孤儿清理、权限撤回均有状态和审计记录。
- **异步可恢复**：回应、回声和模型失败不会伪造成功；有 pending / failed / retry 语义。
- **最小权限**：媒体播放按用户所有权校验；出现在模型上下文前再次校验状态和权限。
- **可解释**：回声显示来源和“为什么现在”；地形显示证据；反馈显示之后改变了什么。
- **可迁移**：当前媒体根目录在 F 盘本地验证，存储层通过 `storage_path` 和媒体 ID 隔离，完成验证后可替换腾讯 COS 适配器。
- **可观测**：上传失败、ASR 失败、Worker 重试、权限撤回和删除级联必须进入结构化日志和指标，而不是只靠前端 toast。

## 当前阶段的验收标准

1. 手机视口可连续完成 9 张普通图片、Live Photo、原始语音和文字的任意组合保存。
2. Live Photo 在生活流显示静态预览，点击可播放动态部分；详情页可再次播放。
3. 删除生活碎片后，静态图、动态视频、原始语音、回应线程、Outbox 和派生记忆均不可再被读取或复活。
4. 关闭 Memory V2 时，已保存生活碎片仍能在生活流稳定完成生命周期，不永久显示 Pending。
5. 反馈后重新打开“我们”页，能看到真实的策略变化，并在后续回应/回声中验证变化生效。

